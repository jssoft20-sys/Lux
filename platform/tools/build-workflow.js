export const meta = {
  name: 'sprintergo-platform',
  description: 'Собрать модули платформы грузоперевозок: ядро фронтенда, бэкенд, три приложения',
  phases: [
    { title: 'Ядро', detail: 'core JS, карта, i18n, UI, расчёт цены, диспетчер, почта' },
    { title: 'Приложения', detail: 'роутеры API и три интерфейса — клиент, курьер, админка' },
    { title: 'Обвязка', detail: 'точка входа, скрипты запуска, nginx, инструкция' },
  ],
}

const P = '/tmp/claude-0/-home-user-Lux/7dca5838-9194-5f06-a843-0c83e0bebcb1/scratchpad/platform'

const PREAMBLE = `Ты собираешь модуль сервиса грузоперевозок Sprinter Go (Бишкек, Кыргызстан).

СНАЧАЛА прочитай ${P}/SPEC.md целиком — это контракт проекта, отклоняться от него нельзя.
Затем прочитай уже готовые файлы ядра, на которые будешь опираться:
  ${P}/server/core.py        — роутер, Ctx, ApiError, HUB (SSE), хелперы bad()/not_found()/...
  ${P}/server/db.py          — схема БД и помощники rows/row/value/execute/insert/update/tx/now/jdump/jload
  ${P}/server/settings.py    — настройки: get/get_int/get_float/get_bool/put/put_many/public
  ${P}/web/assets/css/tokens.css  — переменные дизайна
  ${P}/web/assets/css/fonts.css   — шрифты

ЖЕЛЕЗНЫЕ ПРАВИЛА:
- Ноль внешних зависимостей. Python — только стандартная библиотека. JS — только нативные ES-модули,
  без сборщиков, без npm, без CDN, без React/Vue/Leaflet. CSS — руками.
- Комментарии и весь текст интерфейса — по-русски, живым человеческим языком. Никакого канцелярита
  и никаких признаков «это писал ИИ». Имена переменных и функций — латиницей.
- Деньги — целые тыйыны (1 сом = 100 тыйынов). Никаких float в деньгах.
- Время — целые секунды unix UTC.
- Мобильный первым: базовая вёрстка под 360–430 px.
- Никаких TODO, заглушек, «здесь будет реализация». Либо рабочий код, либо не пиши.
- Пиши ТОЛЬКО свои файлы из списка ниже. Чужие файлы не трогай и не создавай.
- Каждый .py файл должен проходить python3 -m py_compile. Проверь это сам перед завершением.
- Каждый .js файл должен проходить проверку синтаксиса через node --check (для ES-модулей
  используй: node --input-type=module --check < файл). Проверь сам.

В конце верни короткий отчёт: какие файлы создал, какие экспорты/эндпоинты в них, и что
осталось на стыке с другими модулями (что ты ожидаешь от них).`

phase('Ядро')

const CORE = [
  {
    label: 'core: api/store/fmt/router',
    prompt: `${PREAMBLE}

ТВОИ ФАЙЛЫ (создать):
  ${P}/web/assets/js/core/api.js
  ${P}/web/assets/js/core/store.js
  ${P}/web/assets/js/core/fmt.js
  ${P}/web/assets/js/core/router.js

api.js — клиент HTTP+SSE ровно с тем API, что описан в SPEC разделе 4:
  api.get/post/patch/del(path, ...) — префикс /api/v1 добавляется внутри, путь передаётся без него.
  Бросает ApiError {code, message, status} (экспортируй класс).
  api.setToken/token — токен в localStorage под ключом sg_token.
  api.onUnauthorized(fn) — вызывается при 401, чтобы приложение увело на вход.
  api.stream(path, {onEvent, onOpen, onError}) — обёртка над EventSource с автопереподключением
  и нарастающей задержкой (1с, 2с, 4с… максимум 30с), возвращает объект с методом close().
  Важно: EventSource не умеет слать заголовки, поэтому для защищённых потоков токен
  передаётся в query как ?token=…  — предусмотри это.
  Ещё: таймаут запроса 20 с через AbortController; понятные русские сообщения при сетевой ошибке.

store.js — createStore(initial): get(), set(patch), on(fn) возвращает отписку,
  select(selectorFn, cb) возвращает отписку.
  set делает поверхностное слияние и уведомляет подписчиков только если что-то реально изменилось.

fmt.js — money(tiyin) даёт «1 250 сом» (неразрывный пробел в разрядах), moneyShort, distance(m)
  даёт «850 м» либо «12,4 км», duration(s) даёт «1 ч 20 мин» либо «8 мин», time/date/dateTime(unix),
  phone('+996755555357') даёт «+996 755 555 357», plate, plural(n, ['минута','минуты','минут']),
  initials(name), timeAgo(unix). Все функции учитывают текущий язык через второй аргумент lang
  (по умолчанию 'ru') — кыргызские варианты тоже предусмотри.

router.js — простой хеш-роутер для SPA: createRouter(routes, {onChange}), routes — объект вида
  '/track/:id': handler. Методы: go(path, {replace}), back(), current(). Работает на hashchange.`,
  },
  {
    label: 'core: ui + components.css',
    prompt: `${PREAMBLE}

ТВОИ ФАЙЛЫ (создать):
  ${P}/web/assets/js/core/ui.js
  ${P}/web/assets/css/base.css
  ${P}/web/assets/css/components.css

base.css — современный сброс, типографика на токенах, утилиты (.row .col .gap-* .grow .center
  .muted .truncate .hide), правила скролла, safe-area, выделения, видимый focus-visible жёлтым,
  overflow-x: clip на body чтобы ничего не вылезало вбок.

components.css — ВСЕ классы компонентов из SPEC раздела 5, реально проработанные:
  .btn (+ --primary --ghost --danger --lg --block --icon), состояния hover/active/disabled/загрузка
  со спиннером; нажатие даёт лёгкое scale(.97) с --ease.
  .field (обёртка), .field__label (плавающая метка, поднимается при фокусе или значении),
  .field__input, .field__hint, .field--err.
  .card, .card--tap. .sheet (нижняя шторка: .sheet__grip .sheet__head .sheet__body .sheet__foot),
  появление снизу с --dur-2 и --ease, затемнение .sheet__scrim.
  .chip и .chip--on, .avatar и .avatar--lg (с инициалами как запасной вариант), .stars,
  .badge (--ok --warn --err --info), .list/.list__row/.list__row--tap,
  .skeleton (пульсация), .toast, .switch, .stepper (минус/значение/плюс), .segmented,
  .progress, .divider, .empty (пустое состояние).
  Всё на переменных из tokens.css, ни одного захардкоженного цвета.

ui.js — экспорты по SPEC:
  el(tag, props, ...children) — props поддерживает className, style-объект, dataset, обработчики
  on*, html (только для своего кода), иначе textContent. children: строки, узлы, массивы, null.
  toast(msg, {type, ms}) — стек тостов сверху, автоскрытие, свайп-закрытие пальцем.
  sheet({title, content, actions, dismissible, fullHeight}) — нижняя шторка: перетаскивание
  за грип вниз закрывает (pointer events, порог 90px или скорость), клик по затемнению закрывает,
  Esc закрывает, блокировка скролла под шторкой, возврат фокуса. Возвращает close и el.
  confirm({title, text, ok, cancel, danger}) даёт Promise<boolean>.
  spinner(el, on), skeleton(el, rows), haptic() (navigator.vibrate если есть).
  mountStars(el, {value, readonly, onChange}).
  Все анимации уважают prefers-reduced-motion через токены длительностей.`,
  },
  {
    label: 'core: движок карты',
    prompt: `${PREAMBLE}

ТВОИ ФАЙЛЫ (создать):
  ${P}/web/assets/js/core/map.js
  ${P}/web/assets/css/map.css

Напиши СВОЙ движок растровой карты — Leaflet и любые библиотеки запрещены.
API ровно как в SPEC разделе 4 (createMap, setView, fitPoints, marker, route, on, destroy).

Требования по качеству — это лицо сервиса, карта должна быть неотличима от нативной:
- Плитки OSM/Carto по шаблону с подстановками {z} {x} {y}, поддержкой {r} (@2x на ретине)
  и {s} (поддомены). Шаблон приходит снаружи в опциях (из /config), не хардкодь.
- Плавное перетаскивание пальцем и мышью (pointer events), инерция после броска с затуханием,
  щипковый зум двумя пальцами с центром между пальцами, двойной тап даёт зум +1,
  колесо мыши даёт плавный зум. Никаких рывков.
- Дробный зум во время жеста, привязка к целому уровню по завершении с анимацией.
- Слой плиток: переиспользование DOM-узлов, предзагрузка соседних, мягкое проявление
  загруженной плитки через переход opacity, удержание плиток предыдущего уровня под новыми
  во время зума, чтобы не было белых дыр.
- Маркеры — обычные HTML-элементы поверх карты, позиционируются через transform: translate3d.
  marker.moveTo(ll, {duration}) плавно анимирует перемещение (нужно для движения машины
  курьера) и разворачивает иконку по направлению движения, если задан heading.
- route(coords) рисует линию через SVG поверх карты, с обводкой (тёмная подложка плюс яркая
  линия сверху), сглаженными стыками, поддержкой пунктира.
- fitPoints вписывает набор точек с отступами, учитывая, что снизу экран закрыт шторкой
  (опция padding с полями top, right, bottom, left).
- Атрибуция обязательна (мелкая надпись в углу) — это требование лицензии OSM и CARTO.
- requestAnimationFrame для всей отрисовки, никаких setInterval на анимацию.
- Полная очистка в destroy(): слушатели, rAF, узлы.
- Работает при смене размера контейнера (ResizeObserver).

map.css — стили слоёв, маркеров, атрибуции, кнопок зума и «моё местоположение».
Маркеры точки А (жёлтая точка), точки Б (флажок), машины курьера — заготовки классов.`,
  },
  {
    label: 'core: i18n + словари ru/ky',
    prompt: `${PREAMBLE}

ТВОИ ФАЙЛЫ (создать):
  ${P}/web/assets/js/core/i18n.js
  ${P}/web/assets/js/core/lang.ru.js
  ${P}/web/assets/js/core/lang.ky.js

i18n.js — t(key, vars), setLang, getLang, onLangChange, tp(n, key) для множественного числа,
  applyTo(root) — проставляет тексты по атрибутам data-t, data-t-ph (placeholder), data-t-aria.
  Язык хранится в localStorage под ключом sg_lang, по умолчанию ru. Если ключа нет — возвращает
  ключ и один раз пишет предупреждение в консоль, чтобы пропажа была заметна.
  Словари импортируются статически, оба сразу — они маленькие.

lang.ru.js и lang.ky.js — ПОЛНЫЕ словари для всех трёх приложений. Ключи по префиксам:
  common.* (да, нет, отмена, сохранить, назад, дальше, готово, загрузка, ошибка, повторить…)
  order.* (весь клиентский сценарий: откуда, куда, подъезд, квартира, этаж, домофон, комментарий,
    выбор тарифа, грузчики, допуслуги, телефон, имя, подтвердить заказ, цена, в пути…)
  track.* (поиск машины, курьер найден, едет к вам, на месте, погрузка, в пути, разгрузка,
    завершён, отменён, оценить, позвонить, написать, отменить заказ)
  courier.* (вход, регистрация, смена, онлайн и оффлайн, новый заказ, принять, отказаться,
    прибыл, начал погрузку, в пути, завершить, заработок за смену, история, профиль, авто)
  admin.* (все разделы: обзор, заказы, курьеры, клиенты, тарифы, допуслуги, настройки,
    почта, диспетчеризация, платежи, карта)
  err.* (сетевые и серверные ошибки человеческим языком)
  status.* (названия статусов заказа)

КЫРГЫЗСКИЙ — САМОЕ ВАЖНОЕ В ТВОЕЙ ЗАДАЧЕ. Пиши как говорят в Бишкеке, а не как переводит
машина. Ориентиры из живой речи: «жүк ташуу» (грузоперевозка), «жүкчү» (грузчик),
«кайдан» и «кайда» (откуда и куда), «баасы» (цена), «унаа» (машина), «кабат» (этаж),
слово «подъезд» так и остаётся подъездом, «мебель» — мебель, а не книжное «эмерек»,
«калькулятор», а не «эсептегич», «заказ» — заказ. Числительные и падежи ставь правильно.
Если сомневаешься в термине — выбирай то слово, которое реально напишет человек в поиске.
Не используй длинное тире — в мобильной выдаче читается плохо.`,
  },
  {
    label: 'сервер: цена и география',
    prompt: `${PREAMBLE}

ТВОИ ФАЙЛЫ (создать):
  ${P}/server/pricing.py
  ${P}/server/geo.py

pricing.py — ЕДИНСТВЕННЫЙ источник истины по деньгам.
  quote(tariff, points, distance_m, duration_s, loaders, extras, waiting_s=0) возвращает словарь
  с полями base, distance, time, loaders, extras (список позиций с подробностями), waiting,
  subtotal, total, commission, courier_payout, и breakdown — список строк для показа
  пользователю (каждая: code, title_ru, title_ky, qty, unit_price, sum).
  Логика: base_price включает included_km и included_min; сверх них — per_km и per_min,
  километры считаются с точностью до 0.1 км, минуты округляются вверх до целой. Грузчики:
  тариф включает loaders_included бесплатно, сверх — loader_hour_price за час, минимум
  loader_min_hours. Ожидание: waiting_free_min бесплатно, дальше waiting_per_min.
  Допуслуги по своему виду: fixed (цена как есть), hourly (цена умножить на часы),
  per_unit (цена на количество), per_floor (цена на этажи).
  Итог не ниже min_price тарифа и не ниже order.min_price из настроек.
  Комиссия: percent или fixed, с зажимом между commission.min и commission.max.
  ВСЁ в целых тыйынах, округление аккуратное, без float-артефактов.
  Функция должна годиться и для предварительного расчёта, и для закрытия заказа
  с фактическим ожиданием.

geo.py — география без зависимостей:
  haversine(a, b) возвращает метры; bbox_contains; road_distance(points) — оценка по прямой,
  умноженная на road_factor из настроек, когда маршрутизатор недоступен.
  suggest(q, lat, lng) возвращает список подсказок адресов. Провайдер из настроек geo.provider:
    nominatim — запрос к https://nominatim.openstreetmap.org/search с параметрами format=jsonv2,
    countrycodes, viewbox и bounded по geo.bbox, accept-language=ru, limit=8, обязательный
    заголовок User-Agent (без него Nominatim блокирует). Кэш ответов в памяти на 10 минут.
    yandex и 2gis — по ключу из настроек; если ключа нет, тихо откатывайся на nominatim.
  reverse(lat, lng) возвращает title и subtitle.
  route(points) возвращает distance_m, duration_s и route в виде массива пар [lat,lng].
    Провайдер osrm: GET {route.url}/route/v1/driving/{lng,lat;…}?overview=full&geometries=polyline —
    декодер polyline5 напиши сам, он короткий. При любой ошибке или таймауте (3 секунды)
    откатывайся на прямую линию с road_factor и средней скоростью: сервис не должен вставать
    из-за чужого API.
  Все сетевые вызовы — через urllib.request с таймаутом, в try/except, с кэшем.
  Ни один сбой внешнего сервиса не должен ронять запрос пользователя.`,
  },
  {
    label: 'сервер: диспетчеризация',
    prompt: `${PREAMBLE}

ТВОЙ ФАЙЛ (создать):
  ${P}/server/dispatch.py

Движок подбора курьера под заказ. Читает настройки dispatch.* (см. SPEC раздел 7).

Функции:
  candidates(order) возвращает отсортированный список кандидатов. Берём курьеров: online=1,
  busy=0, users.status='active', свежая геопозиция (geo_at не старше 120 секунд), расстояние
  до точки погрузки не больше dispatch.radius_m, класс машины подходит под тариф заказа,
  рейтинг не ниже dispatch.min_rating, и которым этот заказ ещё не предлагали и они не отказывались.
  Оценка score — нормированная сумма: близость (чем ближе, тем выше, вес w_distance),
  рейтинг (вес w_rating), ручной приоритет из админки (вес w_priority),
  доля принятых предложений offers_taken к offers_sent (вес w_acceptance).
  У новичка без статистики вместо доли принятых берётся new_courier_boost — иначе новые
  курьеры никогда не получат первый заказ.

  start_search(order_id) — переводит заказ в статус searching и запускает раздачу.
  tick() — один шаг фонового цикла: закрывает протухшие предложения, шлёт следующую партию
  (dispatch.batch курьеров за раунд, срок жизни offer_ttl_s), после max_rounds или
  order.search_timeout_s переводит заказ в expired и сообщает клиенту.
  accept(order_id, courier_id) — атомарно: только один курьер может забрать заказ.
  Используй db.tx() и проверку текущего статуса внутри транзакции, иначе при одновременном
  нажатии двоих оба получат заказ.
  decline(order_id, courier_id), cancel_search(order_id).
  run_loop(stop_event) — фоновый поток, вызывает tick() раз в секунду, ошибки логирует
  и продолжает работу.

  Все изменения публикуй в HUB из core: темы order:{public_id} (для клиента),
  courier:{user_id} (для курьера), admin (для живой карты).
  События: offer, offer_cancelled, order, search_failed.
  Каждое действие пиши в таблицу order_events.`,
  },
  {
    label: 'сервер: почта, платежи, тексты',
    prompt: `${PREAMBLE}

ТВОИ ФАЙЛЫ (создать):
  ${P}/server/mailer.py
  ${P}/server/payments.py
  ${P}/server/i18n_server.py

mailer.py — отправка почты через smtplib по настройкам smtp.* (host, port, secure none/ssl/tls,
  user, pass, from, from_name). Отправка в фоновом потоке через очередь, чтобы медленный SMTP
  не держал HTTP-запрос. Результат каждой отправки — в таблицу mail_log.
  send(to, template, vars, lang='ru') и send_raw(to, subject, html, text).
  test(to) — проверочное письмо для кнопки в админке, отвечает синхронно с внятной ошибкой
  (неверный пароль, недоступен хост, не тот порт — переведи типовые исключения smtplib
  в понятные русские сообщения).
  ШАБЛОНЫ — сделай их действительно красивыми: письмо на тёмном фоне с жёлтым акцентом,
  свёрстанное таблицами и инлайн-стилями (иначе почтовые клиенты всё сломают),
  ширина 600 px, адаптив под телефон, логотип текстом, кнопка-ссылка.
  Обязательно и HTML, и текстовая версия (multipart/alternative).
  Набор шаблонов: courier_welcome (регистрация принята, ждите проверки),
  courier_approved (доступ открыт, ссылка на вход), courier_rejected,
  courier_new_order (новый заказ рядом: адреса, цена, расстояние),
  password_reset (ссылка, действует час), admin_daily (сводка за день),
  order_receipt (клиенту итог по заказу). Каждый на русском и кыргызском.

payments.py — абстракция платежей, провайдер берётся из настроек payment.provider.
  Интерфейс: init_payment(order, amount, return_url), handle_callback(provider, data),
  verify_signature(provider, data).
  Провайдеры: none (выключено — заказ проходит сразу, оплата наличными курьеру),
  manual (админ помечает оплату руками),
  freedompay — по документированному протоколу PayBox и FreedomPay: подпись pg_sig
  считается как md5 от имени скрипта, отсортированных по ключу значений и секрета;
  инициализация через init_payment.php, разбор callback с проверкой подписи.
  Если реквизитов нет — провайдер сам себя выключает и не ломает оформление заказа.
  Идемпотентность: повторный callback по тому же payment_id не должен списывать дважды.

i18n_server.py — тексты сервера на русском и кыргызском: названия статусов заказа,
  сообщения об ошибках, темы и тела писем. Функция t(key, lang, **vars).
  Кыргызский — живой, не калька с русского.`,
  },
]

const coreResults = await parallel(CORE.map(m => () =>
  agent(m.prompt, { label: m.label, phase: 'Ядро' })))

log('Ядро собрано: ' + coreResults.filter(Boolean).length + ' из ' + CORE.length + ' модулей')

phase('Приложения')

const APPS = [
  {
    label: 'API: auth + courier',
    prompt: `${PREAMBLE}

Модули ядра уже написаны — ПРОЧИТАЙ их перед работой, ты вызываешь их функции:
  ${P}/server/pricing.py  ${P}/server/geo.py  ${P}/server/dispatch.py
  ${P}/server/mailer.py   ${P}/server/payments.py  ${P}/server/i18n_server.py

ТВОИ ФАЙЛЫ (создать):
  ${P}/server/auth.py
  ${P}/server/routers/auth.py
  ${P}/server/routers/courier.py

auth.py — хеширование паролей через hashlib.scrypt (соль 16 байт, n=2**14, r=8, p=1,
  формат хранения scrypt$n$r$p$соль_hex$hash_hex, сравнение через hmac.compare_digest),
  создание и проверка сессий (случайный токен 32 байта, в БД хранится только его sha256),
  функция require(ctx, role=None) возвращает пользователя и проставляет ctx.user.
  Токен читается из заголовка Authorization: Bearer, а для SSE — из query параметра token.
  Валидация email и телефона в кыргызском формате (+996XXXXXXXXX, принимать также
  0XXXXXXXXX и приводить к международному виду). Экспортируй также hash_password,
  verify_password, create_session, destroy_session, normalize_phone, valid_email.

routers/auth.py — регистрация курьера (email, пароль не короче 8 символов, имя, телефон,
  марка, госномер, цвет, класс машины, грузоподъёмность), вход, выход, GET /auth/me,
  PATCH /auth/me, восстановление пароля по почте. Ограничение частоты через core.LIMIT:
  5 попыток входа за 5 минут на пару IP плюс email, 3 регистрации в час на IP.
  Если включено security.require_moderation, новый курьер получает статус pending
  и письмо courier_welcome, а войти не может до одобрения — верни понятную ошибку
  с кодом moderation.

routers/courier.py — весь API курьера из SPEC раздела 3: онлайн и оффлайн, приём геопозиции
  (запись в geo_track не чаще раза в 15 секунд, публикация в HUB в темы order:{pid}
  активного заказа и admin), SSE-поток, приём и отказ от предложений через dispatch,
  смена статуса заказа с проверкой допустимости перехода (нельзя из in_transit обратно
  в assigned), учёт ожидания (старт и стоп, накопление waiting_s), завершение заказа
  с пересчётом итоговой цены через pricing с фактическим ожиданием, обновление статистики
  курьера, история заказов и заработок за период.`,
  },
  {
    label: 'API: public + admin',
    prompt: `${PREAMBLE}

Модули ядра уже написаны — ПРОЧИТАЙ их перед работой:
  ${P}/server/pricing.py  ${P}/server/geo.py  ${P}/server/dispatch.py
  ${P}/server/mailer.py   ${P}/server/payments.py  ${P}/server/i18n_server.py

Файл ${P}/server/auth.py пишет другой агент параллельно. Считай, что он даёт функции:
  require(ctx, role=None) возвращает пользователя, hash_password(p), verify_password(p, h),
  create_session(user_id, ua, ip) возвращает токен, normalize_phone(s), valid_email(s).
  Импортируй его из routers как: from .. import auth — и просто пользуйся.

ТВОИ ФАЙЛЫ (создать):
  ${P}/server/routers/public.py
  ${P}/server/routers/admin.py

routers/public.py — клиентский API без авторизации (SPEC раздел 3):
  /config отдаёт settings.public() плюс активные тарифы и допуслуги с названиями на обоих языках.
  /geo/suggest, /geo/reverse, /geo/route — прокси к geo.py с ограничением частоты
  (30 подсказок в минуту на IP) и проверкой длины запроса.
  /price/quote — расчёт без создания заказа.
  POST /orders — создание: нормализовать телефон, найти или создать клиента, ПЕРЕСЧИТАТЬ
  цену на сервере (присланную клиентом игнорировать), сохранить заказ, выдать public_id
  (8 символов из безопасного алфавита без похожих букв) и track_token, инициировать
  оплату если она включена, иначе сразу вызвать dispatch.start_search. Ограничения:
  не больше 3 активных заказов на один телефон, 10 заказов в час на IP.
  GET /orders/{pid} и /orders/{pid}/stream — доступ только по правильному track_token,
  сравнение через hmac.compare_digest. В ответе не отдавать телефон курьера, пока заказ
  не назначен, и никогда не отдавать внутренние id пользователей.
  Отмена (с учётом order.cancel_free_s), оценка курьера (1–5, обновляет rating_sum и rating_count).
  /payments/init и /payments/callback/{provider}.

routers/admin.py — всё из SPEC: настройки (GET и PUT, секреты наружу не отдавать — вместо
  значения признак «задано»), CRUD тарифов и допуслуг с валидацией, список заказов
  с фильтрами и постраничной выдачей, карточка заказа с лентой событий, курьеры
  (одобрить, заблокировать, приоритет, заметка, письмо при смене статуса), клиенты,
  статистика за период (заказов, выручка, комиссия, средний чек, конверсия поиска в заказ,
  топ курьеров, разбивка по тарифам и по часам), SSE живой карты, тест почты и её журнал.
  Все админские эндпоинты — только для роли admin, проверяй на каждом.`,
  },
  {
    label: 'клиентское приложение',
    prompt: `${PREAMBLE}

Модули ядра фронтенда уже написаны — ОБЯЗАТЕЛЬНО прочитай их и используй, не дублируй:
  ${P}/web/assets/js/core/api.js  store.js  fmt.js  router.js  ui.js  map.js  i18n.js  lang.ru.js  lang.ky.js
  ${P}/web/assets/css/base.css  components.css  map.css  tokens.css

ТВОИ ФАЙЛЫ (создать):
  ${P}/web/index.html
  ${P}/web/assets/css/client.css
  ${P}/web/assets/js/client/app.js
  ${P}/web/assets/js/client/order.js
  ${P}/web/assets/js/client/track.js
  ${P}/web/assets/js/client/address.js

Это главный экран сервиса. Ориентир — приложение Яндекс Go: карта во весь экран,
поверх неё нижняя шторка, которая меняет содержимое по шагам. Ощущение должно быть
как у нативного приложения: мгновенный отклик, плавные переходы, ничего не прыгает.

Сценарий:
1. Карта на весь экран, по центру маркер выбора точки. Шторка «Куда везём?»
   с двумя полями: Откуда (по умолчанию определяется по геолокации, с разрешения)
   и Куда. Кнопка с плюсом добавляет промежуточную точку (до order.max_points).
2. Ввод адреса открывает экран поиска: подсказки по мере набора (задержка 250 мс),
   история последних адресов из localStorage, пункт «указать на карте» — возвращает
   к карте с перетаскиваемым маркером и обратным геокодированием.
3. Когда обе точки заданы — карта вписывает маршрут (route и fitPoints с отступом снизу
   под шторку), шторка показывает горизонтальную карусель тарифов: иконка машины,
   название, время подачи, цена. Выбранный подсвечен. Под каруселью строка
   «Грузчики» со степпером и кнопка «Ещё услуги».
4. Допуслуги — шторка со списком: степперы для количественных, переключатели для фиксированных.
   Цена пересчитывается на лету через /price/quote (запрос с задержкой, предыдущий отменяется).
5. Детали адресов: подъезд, квартира, этаж, домофон, комментарий — по кнопке у каждой точки.
6. Телефон и имя, согласие. Кнопка «Заказать за N сом» — крупная, жёлтая, внизу.
7. После создания — экран поиска машины с живой анимацией (пульсирующие круги по карте,
   бегущая строка «ищем машину»), возможность отменить.
8. Курьер найден — карточка: аватар, имя, рейтинг, марка и госномер машины,
   кнопки «Позвонить» и «Написать», время подачи. Машина едет по карте живьём
   (marker.moveTo с плавной анимацией по событиям SSE).
9. Дальше статусы: на месте, погрузка, в пути, разгрузка, завершён. Каждый статус меняет
   шапку шторки и подсказку. В конце оценка курьера звёздами и комментарий.
10. Ссылка на заказ сохраняется в localStorage: при возврате на сайт активный заказ
    открывается сразу.

Требования:
- Всё общение с сервером через core/api.js, живые обновления через api.stream.
- Переключатель языка RU и KY в шапке, весь текст через t().
- Ни одной строки текста в коде мимо словаря.
- Идеально на 360 px: ничего не обрезано, кнопки не меньше 44 px по высоте,
  нижняя панель учитывает --safe-b.
- У каждого экрана должно быть состояние загрузки и состояние ошибки.
- Плавные переходы между шагами шторки (сдвиг и затухание, --dur-2).`,
  },
  {
    label: 'приложение курьера',
    prompt: `${PREAMBLE}

Модули ядра фронтенда уже написаны — прочитай и используй:
  ${P}/web/assets/js/core/api.js store.js fmt.js router.js ui.js map.js i18n.js lang.ru.js lang.ky.js
  ${P}/web/assets/css/base.css components.css map.css tokens.css

ТВОИ ФАЙЛЫ (создать):
  ${P}/web/courier.html
  ${P}/web/assets/css/courier.css
  ${P}/web/assets/js/courier/app.js
  ${P}/web/assets/js/courier/auth.js
  ${P}/web/assets/js/courier/work.js
  ${P}/web/manifest-courier.webmanifest
  ${P}/web/sw-courier.js

Приложение для водителей. Его потом обернут в APK, поэтому оно должно работать
как самостоятельное приложение: устанавливаться как PWA, помнить вход, не терять
состояние при сворачивании.

Экраны:
1. Вход и Регистрация — переключение вкладками. Вход: почта, пароль, «забыли пароль».
   Регистрация: имя, почта, телефон, пароль, марка машины, госномер, цвет, класс
   (соответствует тарифам), грузоподъёмность, габариты кузова. После отправки
   экран «заявка принята, ждём проверки».
2. Смена: крупный переключатель «На линии» и «Не на линии». Когда на линии — карта
   со своей позицией, статистика смены (заказов, заработок), ожидание заказа.
3. Предложение заказа приходит по SSE: полноэкранная карточка с таймером обратного
   отсчёта (кольцо вокруг кнопки), адреса, расстояние до подачи, цена и сколько
   получит курьер на руки, кнопки «Принять» и «Пропустить». Вибрация и звук —
   звук синтезируй через Web Audio, файл не нужен.
4. Активный заказ: карта с маршрутом, карточка клиента (имя, телефон, кнопка позвонить),
   адреса с деталями (подъезд, квартира, этаж, домофон, комментарий), список услуг.
   Крупная кнопка смены статуса внизу меняется по шагам: «Я на месте», «Начал погрузку»,
   «В пути», «На месте выгрузки», «Завершить заказ». Завершение подтверждается свайпом,
   чтобы не нажать случайно. Кнопка «Ожидание» запускает счётчик.
5. История и заработок: список заказов за день, неделю, месяц с суммами, итог сверху.
6. Профиль: данные, машина, рейтинг, выход.

Геолокация: watchPosition с высокой точностью, отправка на сервер раз в 10 секунд,
но только если позиция реально сместилась больше чем на 15 метров — экономим батарею
и трафик. Если вкладка ушла в фон, не глушить отправку, а снизить частоту.

sw-courier.js — сервис-воркер: кэш оболочки приложения (HTML, CSS, JS, шрифты) для
мгновенного запуска и работы при плохой сети; запросы к /api не кэшировать никогда.`,
  },
  {
    label: 'админка',
    prompt: `${PREAMBLE}

Модули ядра фронтенда уже написаны — прочитай и используй:
  ${P}/web/assets/js/core/api.js store.js fmt.js router.js ui.js map.js i18n.js lang.ru.js lang.ky.js
  ${P}/web/assets/css/base.css components.css map.css tokens.css

ТВОИ ФАЙЛЫ (создать):
  ${P}/web/admin.html
  ${P}/web/assets/css/admin.css
  ${P}/web/assets/js/admin/app.js
  ${P}/web/assets/js/admin/pages.js
  ${P}/web/assets/js/admin/forms.js

Панель управления. Ей пользуются с ноутбука, но она обязана быть рабочей и с телефона.
Боковое меню на десктопе, нижняя навигация на телефоне.

Разделы:
1. Обзор — плитки за период (заказов, выручка, комиссия сервиса, средний чек, активных
   курьеров сейчас, конверсия поиска в заказ), график заказов по часам и по дням,
   последние заказы, курьеры на линии.
2. Живая карта — все курьеры на карте в реальном времени через SSE, цветом по состоянию:
   свободен, в заказе, не на линии. Клик по машине открывает карточку курьера.
   Активные заказы отдельным слоем.
3. Заказы — таблица с фильтрами (статус, период, поиск по номеру, телефону, адресу),
   постраничная. Карточка заказа: маршрут на карте, полная разбивка цены, лента событий
   с временем, участники, кнопки ручного вмешательства (назначить курьера, отменить,
   пометить оплаченным).
4. Курьеры — список с рейтингом, статистикой, статусом. Карточка: одобрить и заблокировать,
   ручной приоритет ползунком от -50 до +50 с пояснением, как это влияет на раздачу заказов,
   заметка, история заказов и заработка.
5. Клиенты — список, количество заказов, сумма, блокировка.
6. Тарифы — таблица с созданием и редактированием: все поля из схемы (базовая цена,
   включённые км и минуты, цена за км и за минуту, минималка, ожидание, грузчики,
   габариты кузова, класс, иконка, порядок, активность), названия на обоих языках.
   Рядом с формой живой калькулятор-проверка: задаёшь расстояние и время, видишь
   итоговую цену, чтобы не ошибиться при настройке.
7. Допуслуги — то же самое: вид (фиксированная, почасовая, за единицу, за этаж), цена,
   единица, пределы количества, к каким тарифам применяется.
8. Настройки — вкладками: сервис, комиссия, диспетчеризация (веса ползунками с живым
   пояснением), карта и геокодер, маршрутизатор, платежи, SMTP и письма (с кнопкой
   «отправить проверочное» и журналом отправок), безопасность.

Требования:
- Все суммы вводятся в сомах, а на сервер уходят в тыйынах — сделай это прозрачно
  и не ошибись в обе стороны.
- Любая форма: проверка до отправки, понятные ошибки у полей, защита от двойной отправки,
  подтверждение при уходе с несохранёнными изменениями.
- Таблицы на телефоне превращаются в карточки, а не в горизонтальную прокрутку.
- Тексты через t() на двух языках.`,
  },
]

const appResults = await parallel(APPS.map(m => () =>
  agent(m.prompt, { label: m.label, phase: 'Приложения' })))

log('Приложения собраны: ' + appResults.filter(Boolean).length + ' из ' + APPS.length)

phase('Обвязка')

const glue = await agent(`${PREAMBLE}

Весь сервис уже написан. ПРОЧИТАЙ всё, что есть в ${P}/server и ${P}/web, чтобы точно
знать имена модулей, роутеров и файлов.

ТВОИ ФАЙЛЫ (создать):
  ${P}/app.py
  ${P}/scripts/start.sh
  ${P}/scripts/stop.sh
  ${P}/scripts/status.sh
  ${P}/scripts/restart.sh
  ${P}/scripts/seed.py
  ${P}/deploy/nginx.conf
  ${P}/deploy/sprintergo-platform.service
  ${P}/deploy/install-service.sh
  ${P}/README.md

app.py — собирает приложение: инициализация БД, наполнение каталога, подключение
всех роутеров, регистрация страниц (/ даёт index.html, /courier даёт courier.html,
/admin даёт admin.html), запуск фоновых потоков (цикл диспетчеризации, очередь почты,
уборка БД раз в час), корректная остановка по SIGTERM и SIGINT с закрытием SSE.
Порт из переменной окружения PORT, по умолчанию 7030. Ключ --dev отключает кэш статики.
При первом запуске, если администраторов нет, создаёт администратора со случайным
паролем и печатает его в консоль один раз крупно, чтобы человек не искал, как войти.

scripts — запуск в фоне с pid-файлом и логом, проверка занятости порта, понятные русские
сообщения, вывод адреса для входа. seed.py — администратор по заданной почте и паролю,
по ключу --demo ещё и демо-курьеры с заказами для проверки интерфейса.

deploy/nginx.conf — рабочий конфиг для домена: http2, HSTS, сжатие, длинный кэш
на статику, проксирование на 127.0.0.1:7030. ОБЯЗАТЕЛЬНО: для путей SSE
(/api/v1/*/stream) отключить буферизацию — proxy_buffering off, proxy_read_timeout 3600s,
proxy_http_version 1.1, заголовок Connection пустой. Без этого живые обновления не работают.
Внутри блоков location не использовать add_header, он отменяет заголовки уровня server —
кэш задавать через expires.

README.md — по-русски, для человека, который не программист: что это, как запустить
на сервере за пять минут, как зайти в админку, что настроить в первую очередь
(тарифы, комиссия, SMTP, карта), как добавить курьера, как выпустить HTTPS,
как обновить, где лежат логи и база, как сделать резервную копию.
Отдельный раздел: как позже собрать APK курьера из этого же кода (PWA и обёртка WebView),
с честным указанием, что именно для этого понадобится.`, { label: 'точка входа и деплой', phase: 'Обвязка' })

return {
  core: coreResults.filter(Boolean).length,
  apps: appResults.filter(Boolean).length,
  glue: glue ? 'ok' : 'не собрано',
}
