export const meta = {
  name: 'sprintergo-v3',
  description: 'Оплата по QR Оптима, бонусы, детали адресов, починка карты, отчёты и фишки',
  phases: [
    { title: 'Сервер', detail: 'оплата Оптима, бонусы, картинка для ссылки' },
    { title: 'Экраны', detail: 'карта, адреса, оплата, клиент, курьер, админка' },
  ],
}

const P = '/tmp/claude-0/-home-user-Lux/7dca5838-9194-5f06-a843-0c83e0bebcb1/scratchpad/platform'

const BASE = `Проект: сервис грузоперевозок Sprinter Go, Бишкек, работает на sprintergo.kg/go/.
Прочитай ${P}/SPEC.md — контракт. Прочитай целиком каждый файл, который правишь.

ЖЕЛЕЗНЫЕ ПРАВИЛА:
- Ноль внешних зависимостей: Python только стандартная библиотека, JS только нативные
  ES-модули. Никаких библиотек, сборщиков, CDN. CSS руками.
- Комментарии и текст интерфейса по-русски, живым языком. Имена в коде латиницей.
- Деньги целые тыйыны, время целые секунды unix UTC.
- Мобильный первым, 360-430 px, кнопки не мельче 44 px.
- Пиши ТОЛЬКО свои файлы. Чужие правят параллельно — не трогай.
- Никаких TODO и заглушек.
- Синтаксис проверь сам: python3 -m py_compile для .py,
  node --input-type=module --check < файл для .js.

СТРОКИ: общий словарь lang.ru.js не трогай, его правят другие. Свои строки объявляй
в своём модуле: import { t, extend } from '../core/i18n.js';
extend({ ru: {'pay.title':'Оплата'}, ky: {'pay.title':'Төлөм'} });
Кыргызский живой, как говорят в Бишкеке, не машинный перевод.

В конце верни короткий отчёт: что сделал и что ждёшь от соседних модулей.`

const OPTIMA = `ДОКУМЕНТАЦИЯ OPTIMA BUSINESS (выверено по официальному руководству,
версия 1.0 от 12.06.2026 — считай это единственным источником правды, не додумывай):

Базовый URL: https://api.optimabusiness.kg/
Формат: JSON. Аутентификация: заголовок  X-API-KEY: <ключ>

1) Торговые точки и кассы — их НЕ вводят руками, они определяются сами:
   GET /api/v2/get-sale-point-infos/{legalPartyId}
   Заголовки: x-api-key, Content-Type: application/json
   Ответ — массив:
   [ { "account": "1234567890",
       "salePointInfoDtoList": [
         { "name": "Точка А", "address": "Московская N", "code": 101,
           "cashDtoList": [ {"name":"Касса 1","code":1001}, {"name":"Касса 2","code":1002} ] } ] } ]

2) Генерация QR (используем только v2, банк прямо пишет что v1 доживает):
   POST /api/v2/generate/qr
   Заголовки: Content-Type: application/json, X-API-KEY
   Тело:
   { "requisite": { "salePointCode": 1, "cashCode": 1, "legalPartyId": 248 },
     "sum": 1500.50, "note": "Оплата услуг",
     "qrGenerateType": "CALLBACK_WEB_PARTNER_BY_SALE_POINT",
     "transactionCount": 1, "untilDateTime": "", "qrType": "png", "qrSize": 300,
     "payerClientType": "", "extraInfo": { } }
   Ответ: { "qrBase64": "...", "qrUrl": "https://...", "transactionId": "123" }
   Важно: sum — число с точкой, до 2 знаков после запятой, в СОМАХ, не в тыйынах.
   Типы qrGenerateType с поддержкой обратных уведомлений:
   PLATFORM_PARTNER_BY_SALE_POINT, DEEP_LINKS_BY_SALE_POINT,
   CALLBACK_WEB_PARTNER_BY_SALE_POINT. Берём CALLBACK_WEB_PARTNER_BY_SALE_POINT.

3) Обратное уведомление об оплате — банк стучится К НАМ:
   POST https://наш-домен/api/v1/callback   (путь у нас будет свой, см. задачу)
   Аутентификация: Basic Auth, заголовок Authorization: Basic <base64 логин:пароль>
   Тело от банка:
   { "status": "PROCESSED", "sum": 100.50, "note": "Оплата услуг",
     "transactionProcessedDateTime": "2025-03-12T14:30:00Z",
     "transactionId": "1234567890", "payerClientType": "" }
   Мы обязаны ответить 200 OK и телом:
   { "message": "Callback успешно обработан", "transactionId": "...", "receivedAt": "..." }
   При ошибке — 400, 401 или 500.

4) Проверка статуса вручную (на случай если уведомление не дошло):
   GET /api/v1/get-qr-transaction-info/{qrTransactionId}   (без тела)

5) Остатки по счетам: GET /api/v1/get-account-infos-by-filter — пригодится для отчётов.`

phase('Сервер')

const SERVER = [
  {
    label: 'сервер: оплата по QR Оптима',
    prompt: `${BASE}

${OPTIMA}

ТВОИ ФАЙЛЫ:
  ${P}/server/payments.py       (переписать под Оптиму, старую абстракцию сохранить)
  ${P}/server/routers/pay.py    (создать: эндпоинты оплаты и настройки в админке)

ЗАДАЧА. Клиент платит не всю сумму заказа, а только комиссию сервиса — это бронь,
доказательство что человек настоящий, остальное он отдаёт курьеру наличными.
Размер брони — комиссия сервиса из настроек, но не меньше payment.prepay_min
и не больше payment.prepay_max (заведи их со здравыми значениями).

1. Провайдер 'optima' в payments.py:
   - настройки: payment.optima_key (X-API-KEY), payment.optima_company (legalPartyId),
     payment.optima_sale_point, payment.optima_cash — последние два заполняются
     автоматически из get-sale-point-infos, руками их никто не вводит;
   - sale_points(): тянет точки и кассы, кэширует на час;
   - init_payment(order, amount_tiyin): генерирует QR v2, кладёт в базу transactionId,
     возвращает {qr_base64, qr_url, transaction_id, sum};
   - check_status(transaction_id): дёргает get-qr-transaction-info, нормализует ответ
     в 'pending' | 'paid' | 'failed';
   - handle_callback(data, auth_header): проверяет Basic Auth по
     payment.callback_login и payment.callback_password, сверяет сумму с ожидаемой
     (расхождение больше тыйына — отказ), ИДЕМПОТЕНТНО помечает заказ оплаченным.
     Повторное уведомление по тому же transactionId не должно платить дважды.
   Секреты наружу не отдавать никогда.

2. routers/pay.py с функцией register(app):
   POST /pay/{public_id}/qr?t=токен   — создать или вернуть уже созданный QR заказа
   GET  /pay/{public_id}/status?t=    — состояние оплаты для опроса с экрана
   POST /pay/callback                 — приём уведомления банка (Basic Auth)
   GET  /admin/pay/sale-points        — список точек и касс для админки
   PUT  /admin/pay/settings           — сохранить ключ, ID компании, выбранную точку;
        при сохранении САМ подтягивает точки и, если она одна, выбирает её без вопросов
   POST /admin/pay/test               — проверка связи с банком, понятный ответ
   Ограничение частоты на опрос статуса: не чаще раза в 2 секунды на заказ.
   После подтверждения оплаты публикуй в HUB событие 'order' в тему order:{public_id},
   чтобы экран клиента переключился сам, без нажатий.

3. Заказ не уходит в поиск машины, пока бронь не оплачена, — но только если
   payment.enabled и выбран провайдер. Иначе всё работает как раньше.`,
  },
  {
    label: 'сервер: бонусы и предоплата',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ:
  ${P}/server/bonus.py      (создать)
  ${P}/server/pricing.py    (править)

1. БОНУСНАЯ СИСТЕМА. Придумай её сам так, чтобы она работала на повторные заказы,
   а не раздавала деньги даром. Разумная основа:
   - кэшбек с каждого выполненного заказа, процент из настроек bonus.percent
     (по умолчанию 3), начисляется когда заказ завершён, а не когда создан;
   - бонусами можно закрыть не больше bonus.max_share процентов заказа (по умолчанию 30) —
     иначе сервис работает в минус;
   - бонусы сгорают через bonus.expire_days дней без заказов (по умолчанию 180),
     человека надо предупредить за неделю;
   - приглашение друга: у каждого клиента свой код, друг получает bonus.invite_friend
     на первый заказ, пригласивший bonus.invite_owner после первого выполненного
     заказа друга. Защита от накрутки: начисление только после ЗАВЕРШЁННОГО заказа
     друга, один телефон — один раз, самому себе нельзя;
   - за оценку заказа маленький бонус, чтобы отзывы вообще писали.
   Таблицы создай сам через ту же миграцию, что в db.py (ADDED_COLUMNS и SCHEMA там
   уже есть — НЕ правь db.py, создай свои таблицы из bonus.py при первом обращении,
   CREATE TABLE IF NOT EXISTS).
   Функции: balance(client_id), history(client_id), accrue(...), spend(...),
   max_spendable(client_id, order_total), expire_old(), invite_code(client_id),
   apply_invite(client_id, code). Каждое движение бонусов — строкой в журнале
   с причиной, чтобы в админке было видно, откуда взялось.

2. В pricing.py добавь в расчёт:
   - позицию «от двери до двери» (код door_to_door): когда включена, к цене
     добавляется фиксированная надбавка из настроек price.door_to_door
     (по умолчанию 15000 тыйынов = 150 сом) за КАЖДУЮ точку, где она включена.
     Смысл: курьер поднимается к двери, а не ждёт у машины;
   - списание бонусов: отдельной строкой со знаком минус, не больше max_share;
   - расчёт размера предоплаты (брони): комиссия сервиса, зажатая между
     payment.prepay_min и payment.prepay_max. Отдай её в ответе полем prepay.
   Ничего не сломай: старые вызовы quote() должны работать как раньше.`,
  },
  {
    label: 'сервер: картинка для ссылки на заказ',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ:
  ${P}/server/share.py   (создать, с функцией register(app))

Когда человек делится ссылкой на заказ в WhatsApp или Телеграме, там должна
появляться красивая карточка, а не голая ссылка.

1. GET /share/{public_id}.png — картинка 1200×630 для превью.
   Рисуй САМ, без библиотек: собери PNG вручную (zlib есть в стандартной библиотеке,
   формат PNG несложный — заголовок, IHDR, IDAT со сжатыми строками, IEND).
   Проще всего рисовать в память как массив пикселей RGB и потом упаковать.
   На картинке: тёмный фон фирменного цвета, крупно «Заказ в пути» или статус,
   адреса откуда и куда, расстояние и время, марка машины и госномер если назначена,
   логотип Sprinter Go. Шрифт рисовать нечем — сделай простой растровый шрифт
   прямо в коде (таблица глифов для русских букв и цифр, 5x7 или крупнее с масштабом).
   Если это выходит слишком грубо, вместо текста нарисуй понятную схему маршрута:
   две точки, линия между ними, подписи цифрами. Главное — картинка должна быть
   осмысленной, а не пустым прямоугольником. Кэшируй результат на минуту.

2. GET /share/{public_id} — HTML-страница превью с мета-тегами Open Graph и Twitter:
   og:title, og:description, og:image (наша картинка), og:url, og:type=website.
   Страница сама по себе тоже должна быть человеческой: статус заказа, адреса,
   время, кнопка «Открыть отслеживание». Токен отслеживания в неё НЕ вшивать —
   показывать только то, что не стыдно показать постороннему: статус, город,
   примерное время. Ни телефонов, ни точных адресов квартиры, ни имени клиента.

3. Мессенджеры ходят за превью без cookie и без токена — учти это: страница и
   картинка доступны по одному public_id, поэтому в них не должно быть ничего
   чувствительного, а сам public_id уже случайный.`,
  },
]

const srv = await parallel(SERVER.map(m => () => agent(m.prompt, { label: m.label, phase: 'Сервер' })))
log('Сервер: ' + srv.filter(Boolean).length + ' из ' + SERVER.length)

phase('Экраны')

const SCREENS = [
  {
    label: 'карта: серые дыры при отдалении',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ:
  ${P}/web/assets/js/core/map.js
  ${P}/web/assets/css/map.css

НАСТОЯЩИЙ БАГ, воспроизведён. Владелец пишет: «карта прогоняет, когда отдаляю
карта серая». Я замерил при восьми нажатиях на минус:

  отдаление 1: всего 48 плиток, загружено 48, видимых 24, уровни 16
  отдаление 5: всего 24, загружено 24, видимых 8,  уровни 13
  отдаление 7: всего 108, загружено 61, видимых 82, уровни 13,12,11
  отдаление 8: всего 99,  загружено 49, видимых 76, уровни 12,11,10

На снимке экрана сверху и по краям пустые серые полосы: видимых плиток вдвое
больше, чем загруженных. Плитки 2ГИС на всех масштабах от z=3 до z=18 отдаются
кодом 200 — дело не в поставщике, а в движке.

Причина в том, что при отдалении область видимости в плитках растёт, а движок
не добирает их на новый уровень достаточно быстро и с достаточным запасом,
и нижние уровни не покрывают открывшуюся площадь.

Почини так, чтобы серых дыр не было НИКОГДА:
- считай сетку нужных плиток от текущего вида, а не от старого центра, и бери
  запас в одну плитку за краем экрана;
- держи постоянный грубый подслой на несколько уровней ниже текущего: он
  весит копейки, зато мгновенно закрывает дыры при любом движении;
- при смене уровня не выбрасывай старый, пока новый не закрыл экран целиком;
- ограничь минимальный масштаб разумным значением (город целиком и чуть шире),
  дальше отдалять бессмысленно;
- плитку, которая не загрузилась, пробуй ещё раз с задержкой, но не больше двух раз.
Проверь сам: открой карту, нажми минус восемь раз и убедись, что загруженных
плиток не меньше видимых на каждом шаге.

Заодно доведи плавность: зум должен идти одним непрерывным движением, без рывка
на границе уровней, и при жесте двумя пальцами тоже.`,
  },
  {
    label: 'клиент: детали адресов и от двери до двери',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ:
  ${P}/web/assets/js/client/order.js
  ${P}/web/assets/js/client/address.js

1. НОВЫЙ ШАГ ПОСЛЕ АДРЕСОВ. Когда обе точки заданы, человек должен уточнить детали
   по каждой: подъезд, квартира или офис, этаж, домофон, комментарий курьеру.
   Сейчас это спрятано, сделай отдельным понятным шагом со списком точек:
   у каждой — адрес, под ним заполненные детали одной строкой, тап открывает
   форму. Незаполненное не мешает заказать, но подсказка должна объяснять,
   зачем это нужно: «курьер найдёт вас быстрее».

2. ПЕРЕКЛЮЧАТЕЛЬ «ОТ ДВЕРИ ДО ДВЕРИ» у каждой точки. Включён — курьер поднимается
   к двери, а не ждёт у машины, и это стоит дороже. Цена приходит с сервера
   (позиция door_to_door в расчёте, её делает другой агент в pricing.py) —
   покажи надбавку рядом с переключателем, чтобы не было сюрпризов.
   Выключен — курьер ждёт у подъезда, так дешевле. Оба варианта опиши одной
   короткой строкой, без инструкций на три абзаца.
   Если человек указал этаж выше первого и лифта нет, мягко подскажи, что
   с «от двери до двери» будет проще.

3. Пересчёт цены при каждом изменении — мгновенный и плавный, запрос с задержкой,
   предыдущий отменяется, пока считается — старое значение приглушено, а не пропадает.`,
  },
  {
    label: 'клиент: оплата по QR',
    prompt: `${BASE}

${OPTIMA}

ТВОИ ФАЙЛЫ (создать):
  ${P}/web/assets/js/client/pay.js
  ${P}/web/assets/css/pay.css

Сервер уже готов — прочитай ${P}/server/routers/pay.py и ${P}/server/payments.py.
Эндпоинты: POST /pay/{pid}/qr?t=, GET /pay/{pid}/status?t=.

ЭКРАН ОПЛАТЫ. Клиент платит только комиссию сервиса — это бронь, остальное
наличными курьеру. Объясни это одной строкой, человек не должен гадать.

1. КРУПНЫЙ QR ПО ЦЕНТРУ ЭКРАНА, в центре самого кода — логотип Sprinter Go
   в белом круге. Банк возвращает готовую картинку в qrBase64 — показывай её,
   а логотип накладывай сверху по центру средствами вёрстки. Код должен читаться:
   логотип не больше пятой части стороны, вокруг него белое поле.
   Под кодом — сумма крупно и подпись «Отсканируйте в приложении банка».
   Кнопка «Открыть в приложении банка» по ссылке qrUrl, если она пришла.
   Кнопка «Сохранить код» — скачивает картинку.

2. АВТОПРОВЕРКА. Экран сам опрашивает GET /pay/{pid}/status раз в 3 секунды
   и одновременно слушает поток событий заказа: как только банк подтвердил,
   экран сам переключается на поиск машины. Никаких «нажмите, если оплатили».
   Пока ждём — спокойная анимация вокруг кода, не мигающая.
   Оплата подтверждена — короткая радостная анимация (галочка рисуется линией),
   лёгкая вибрация, и через полторы секунды переход дальше.
   Не дождались за 10 минут — предложить обновить код.

3. Состояния: код создаётся, ждём оплату, оплачено, ошибка связи с банком,
   оплата отклонена. У каждого — понятный текст и что делать дальше.
   Экран не должен «залипать»: если что-то пошло не так, из него всегда есть выход.`,
  },
  {
    label: 'клиент: бонусы, профиль, ссылка',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ:
  ${P}/web/assets/js/client/app.js
  ${P}/web/assets/js/client/track.js

Сервер готов: бонусы в ${P}/server/bonus.py, карточка для ссылки в
${P}/server/share.py — прочитай оба, эндпоинты там.

1. БОНУСЫ В ПРОФИЛЕ: баланс крупно, сколько сгорит и когда, история начислений
   и списаний понятными строками («кэшбек с заказа», «пригласили Азамата»),
   свой код приглашения с кнопкой «Поделиться» и объяснением выгоды в одну строку.

2. СПИСАНИЕ ПРИ ЗАКАЗЕ: переключатель «Списать бонусы» с суммой, которую реально
   можно списать по этому заказу, и итогом к оплате. Если бонусов мало —
   покажи, сколько не хватает, но не выключай переключатель молча.

3. ПОДЕЛИТЬСЯ ЗАКАЗОМ: кнопка отдаёт ссылку вида /go/share/{public_id}.
   Через navigator.share, где он есть, иначе копирование в буфер с тостом.
   Объясни, что увидит получатель: статус и время, без телефона и точного адреса.

4. ФИШКИ, чтобы людям было удобно и не надоедало. Сделай то, что реально помогает,
   а не украшает:
   - подсказка при первом заходе, короткая, один экран, с возможностью пропустить;
   - «Повторить последний заказ» одной кнопкой на главном экране, если заказы были;
   - любимые адреса: дом и работа, ставятся в один тап из истории;
   - когда курьер едет, на экране видно живое время подачи, а не статичная цифра;
   - если человек открыл сайт во время активного заказа — сразу отслеживание.
   Всё это должно работать на телефоне за одно касание.`,
  },
  {
    label: 'курьер: заработок, цели, удобство',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ:
  ${P}/web/assets/js/courier/work.js
  ${P}/web/assets/js/courier/app.js

Водители не читают инструкций. Всё должно быть понятно с одного взгляда за рулём.

1. ЭКРАН СМЕНЫ: крупно заработок за сегодня, рядом — сколько заказов и часов
   на линии. Цель на день, которую водитель ставит сам, и полоса прогресса к ней:
   это заметно держит людей на линии. Цель хранится у него же на телефоне.

2. ЗАРАБОТОК: за день, неделю, месяц. Простой столбчатый график по дням
   (рисуй сам, без библиотек), средний чек, лучший день, сколько вышло на руки
   после комиссии. Внизу список заказов с суммами.

3. В РАБОТЕ УДОБСТВО:
   - крупные кнопки, ничего мельче 56 px на экране активного заказа;
   - адрес следующей точки всегда виден сверху, одной строкой, крупно;
   - тап по адресу копирует его, долгое нажатие открывает навигатор;
   - телефон клиента в один тап, рядом кнопка чата с числом непрочитанных;
   - если курьер стоит на месте больше пяти минут при статусе «в пути» —
     мягкое напоминание проверить, не забыл ли он сменить статус;
   - предупреждение перед выходом с линии, если есть активный заказ.

4. Ничего не мигает и не прыгает: водитель смотрит на экран урывками.`,
  },
  {
    label: 'админка: реквизиты, отчёты, бонусы',
    prompt: `${BASE}

${OPTIMA}

ТВОИ ФАЙЛЫ:
  ${P}/web/assets/js/admin/pages.js
  ${P}/web/assets/js/admin/forms.js
  ${P}/web/assets/js/admin/app.js
  ${P}/web/assets/css/admin.css

Сервер готов — прочитай ${P}/server/routers/pay.py и ${P}/server/bonus.py.

1. РАЗДЕЛ «ОПЛАТА». Владелец вводит РОВНО ДВА поля: API-ключ и ID компании
   (legalPartyId). Всё остальное подтягивается само: по кнопке «Проверить связь»
   тянутся торговые точки и кассы через get-sale-point-infos, показываются списком
   с адресами, и если точка одна — выбирается молча. Несколько — выбор из списка.
   Ключ после сохранения не показывать, только признак «задан» и кнопку заменить.
   Рядом — размер брони: процент комиссии и границы, с живым примером
   «с заказа на 1500 сом клиент заплатит вперёд 150 сом».
   Логин и пароль для обратного уведомления банка — сгенерировать кнопкой,
   показать один раз с пояснением, куда их вписать в кабинете Оптимы,
   и адрес нашего обработчика, который туда нужно указать.

2. РАЗДЕЛ «БОНУСЫ»: проценты, потолок списания, срок сгорания, суммы за приглашение
   и за отзыв — всё ползунками и полями с живым пояснением, что это значит для денег.
   Ниже — сколько бонусов выдано, списано и висит сейчас, и последние движения.

3. ОТЧЁТЫ — то, ради чего владелец заходит каждый день:
   - выручка, комиссия сервиса, средний чек, число заказов за период с выбором дат;
   - разбивка по тарифам, по часам суток и по дням недели — видно, когда ставить
     больше машин;
   - топ курьеров: заказы, выручка, рейтинг, доля отказов от предложений;
   - клиенты: новые против вернувшихся, сколько заказов на клиента;
   - отмены с причинами и на каком шаге отменяли;
   - выгрузка любого отчёта в CSV одной кнопкой (собери файл на клиенте
     из тех же данных, Blob и ссылка на скачивание).
   Графики рисуй сам: аккуратные столбцы и линии, подписи осей, значения при
   наведении. Никаких библиотек.

4. Всё это должно быть рабочим и с телефона: таблицы превращаются в карточки.`,
  },
]

const scr = await parallel(SCREENS.map(m => () => agent(m.prompt, { label: m.label, phase: 'Экраны' })))
log('Экраны: ' + scr.filter(Boolean).length + ' из ' + SCREENS.length)

return { сервер: srv.filter(Boolean).length, экраны: scr.filter(Boolean).length }
