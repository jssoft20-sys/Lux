export const meta = {
  name: 'sprintergo-v4',
  description: 'Дизайн как у Яндекса, живые шторки, карта, экраны клиента и курьера, админка, приложение для Android',
  phases: [
    { title: 'Оболочка', detail: 'дизайн-система, шторки, навигация, сплэш, установка' },
    { title: 'Экраны', detail: 'карта, заказ, профиль, отслеживание, курьер, админка' },
    { title: 'Своё', detail: 'QR-кодер, живая ссылка, приложение для Android' },
  ],
}

const P = '/tmp/claude-0/-home-user-Lux/7dca5838-9194-5f06-a843-0c83e0bebcb1/scratchpad/platform'
const REF = '/tmp/claude-0/-home-user-Lux/7dca5838-9194-5f06-a843-0c83e0bebcb1/scratchpad/zipref'

const BASE = `Проект: Sprinter Go — грузоперевозки по Бишкеку, работает на sprintergo.kg/go/.
Прочитай ${P}/SPEC.md. Прочитай целиком каждый файл, который правишь.

ЖЕЛЕЗНЫЕ ПРАВИЛА:
- Ноль внешних зависимостей: Python только стандартная библиотека, JS только нативные
  ES-модули. Никаких библиотек, сборщиков, CDN. CSS руками.
- Комментарии и текст интерфейса по-русски, живым языком, объясняют «зачем», а не «что».
  Имена в коде латиницей.
- Деньги целые тыйыны, время целые секунды unix UTC.
- Мобильный первым, 360-430 px, кнопки не мельче 44 px (у курьера 56).
- Пиши ТОЛЬКО свои файлы. Чужие правят параллельно — не трогай, даже одну строку.
- Никаких TODO и заглушек. Никаких «примерно так».
- Синтаксис проверь сам: python3 -m py_compile для .py,
  node --input-type=module --check < файл для .js.

СТРОКИ: общий словарь lang.ru.js / lang.ky.js не трогай. Свои строки объявляй
в своём модуле: import { t, extend } from '../core/i18n.js';
extend({ ru: {'pay.title':'Оплата'}, ky: {'pay.title':'Төлөм'} });
Кыргызский живой, как говорят в Бишкеке, не машинный перевод.

В конце верни короткий отчёт: что сделал и что ждёшь от соседних модулей.`

const LOOK = `КАК ДОЛЖНО ВЫГЛЯДЕТЬ. Владелец прислал снимки Яндекс Go и написал:
«у яндекса дизайн цвета кнопки инпуты иконки и тд все реальное живое крутое
так и хочется пользоваться сервисом. Сделай так же крутым у меня».

Снимки лежат в ${REF}/ — ПОСМОТРИ их инструментом Read, прежде чем писать код.
Самые полезные:
  IMG_6080.png — заставка при запуске: жёлтый фон во весь экран, крупный чёрный знак
  IMG_6082.png, IMG_6101.png — пояснительный экран: сливочный или пастельный фон,
      огромный жирный заголовок, текст, картинка, чёрная кнопка-таблетка внизу
  IMG_6093.png, IMG_6098.png — экран заказа: карточки со скруглением 20-24 px,
      сегментный переключатель S M L XL XXL, строка «Грузчики» с переключателем
      «Нет / 1», чипы с (i), строки адресов с кнопкой «Детали» справа,
      жёлтая кнопка во всю ширину внизу
  IMG_6105.png — поиск адреса: совпавшая часть подсвечена синим, расстояние справа
  IMG_6103.png, IMG_6104.png, IMG_6106.png, IMG_6094.png — остальные экраны

ЧТО ИЗ ЭТОГО БЕРЁМ:
- Светлая тема по умолчанию. Тёмная остаётся и работает целиком, но открывается
  сервис в светлой. Владелец сказал прямо: «всегда светлая тема дефолт».
- Крупные скругления (20-24 px у карточек, 16 px у полей, таблетки у кнопок).
- Плотные группы: несколько строк в одной карточке с разделителями, а не
  десять отдельных карточек с зазорами.
- Сегментные переключатели вместо выпадающих списков там, где вариантов до пяти.
- Чипы-таблетки для второстепенных действий.
- Заголовки крупные и жирные, подписи спокойные серые.
- Нажатие ощущается: кнопка проседает, у карточки отклик, лёгкая вибрация.
  Владелец: «Сайт живым сделай когда что-то нажимаешь двигаешь чтобы чувствовалось».
НЕ копируй чужие картинки и логотипы — берём язык формы, а не имущество Яндекса.`

phase('Оболочка')

const SHELL = [
  {
    label: 'оболочка: дизайн-система и живые шторки',
    prompt: `${BASE}

${LOOK}

ТВОИ ФАЙЛЫ:
  ${P}/web/assets/css/tokens.css
  ${P}/web/assets/css/base.css
  ${P}/web/assets/css/components.css
  ${P}/web/assets/js/core/ui.js

ТЫ ДЕЛАЕШЬ ОБОЛОЧКУ, КОТОРОЙ ПОЛЬЗУЮТСЯ ВСЕ ОСТАЛЬНЫЕ. Соседние агенты в этот
же час правят экраны и зовут твои sheet(), dockSheet(), toast(), el(). Значит:
существующие имена и сигнатуры не ломаем, только добавляем. Если меняешь
поведение — оно должно улучшать все экраны разом, без правок в них.

1. СВЕТЛАЯ ТЕМА ПО УМОЛЧАНИЮ. Сейчас основная тёмная. Переверни: на голом :root
   светлые значения, тёмные — в @media (prefers-color-scheme: dark) с защитой
   :root:not([data-theme="light"]) и в :root[data-theme="dark"]. Ни один цвет не
   должен иметь единственное определение внутри media-блока.
   Светлая палитра в духе снимков: фон сливочный (не белый), карточки белые,
   текст почти чёрный, жёлтый акцент как есть, чёрные кнопки-таблетки для
   главных действий на пояснительных экранах.

2. ШТОРКИ ТЯНУТСЯ В ОБЕ СТОРОНЫ. Сейчас sheet() умеет только вниз — закрыться.
   Владелец: «когда тянешь любое модальное окно верх снизу пусто чтобы там не
   было так а растягивалось куда доступно».
   Сделай три положения: по содержимому (как сейчас), развёрнуто на весь
   доступный экран, закрыто. Тянут за грип или шапку; отпустили — шторка сама
   доезжает до ближайшего положения с учётом скорости броска. Вверх дальше
   доступной высоты не пускать, резинового провала быть не должно.
   Новый параметр sheet({ expandable: true }) включает это; без него старое
   поведение. Добавь метод api.expand() / api.collapse().

3. ПОЛНОЭКРАННЫЕ ДЕТАЛИ. sheet({ full: true }) — шторка сразу во весь экран,
   с шапкой, крестиком и прокруткой только внутри тела. Владелец: «модальное
   окно у курьера когда уже заказ принят и тд детали заказа на полный экран и у
   клиента а то листать приходится неудобно».

4. ПОТЯНУТЬ СВЕРХУ — ОБНОВИТЬ. Экспортируй pullToRefresh(scrollEl, onRefresh):
   тянем вниз от самого верха — появляется и доворачивается значок, отпустили —
   вызывается onRefresh, пока он работает значок крутится. Владелец: «в пва нету
   же обновлении придется закрыть и открыть приложение».
   ВАЖНО, он на это отдельно жалуется: «если модальное окно открыто то листаешь
   бывает обновление баг срабатывает». Значит: жест работает только когда
   прокрутка ровно в нуле, только при вертикальном движении, и НЕ работает,
   когда открыта шторка, идёт жест по карте или палец начал движение внутри
   прокручиваемого блока, который сам не в нуле.

5. ЖИВОЕ НАЖАТИЕ. Кнопки, строки списков, чипы, карточки: при нажатии заметно
   проседают (transform: scale) и возвращаются с лёгким перелётом. Отклик
   мгновенный — на pointerdown, а не на click. Никаких задержек 300 мс.
   Уважай prefers-reduced-motion: там всё то же, но без движения.

6. ОБЩИЕ КИРПИЧИ, которых не хватает экранам (объяви и опиши в комментарии):
   segmented(items, opts) — сегментный переключатель как на снимках;
   chip(text, opts) — чип-таблетка, с (i) при желании;
   rowGroup(rows) — группа строк в одной карточке с разделителями;
   infoStory({title, text, art, cta}) — пояснительный экран как IMG_6082;
   pressable(node) — навесить живое нажатие на что угодно.
   Экраны будут их звать, поэтому имена именно такие.

Проверь сам в браузере (playwright есть, /opt/node22/lib/node_modules/playwright):
подними сервер, открой клиента, потяни шторку вверх и вниз, проверь, что
светлая тема включилась и в ней ничего не потерялось.`,
  },
  {
    label: 'оболочка: навигация, заставка, установка приложения',
    prompt: `${BASE}

${LOOK}

ТВОИ ФАЙЛЫ:
  ${P}/web/assets/js/core/router.js
  ${P}/web/assets/js/core/shell.js            (создать)
  ${P}/web/assets/css/shell.css               (создать)
  ${P}/web/index.html
  ${P}/web/courier.html
  ${P}/web/admin.html
  ${P}/web/manifest.webmanifest               (создать, для клиента)
  ${P}/web/sw-client.js                       (создать)

1. ЗАПОМИНАНИЕ МЕСТА. Владелец: «когда открыт раздел профиль и тд в адресе
   домена там /profile и тд у админки клиента курьера в разделах чтобы при
   перезагрузке сайта и тд сохранялось где он был в последний момент».
   Роутер уже хеш-адресный. Чего не хватает: открытая шторка (профиль, бонусы,
   история, детали заказа) в адресе не отражается, и перезагрузка её теряет.
   Добавь в router.js понятие «наложения»: router.overlay('profile') ставит
   #/…/~profile, router.closeOverlay() снимает. При загрузке роутер сообщает
   приложению, какое наложение надо открыть. Экраны позовут это сами — твоя
   задача дать механизм и описать его в шапке файла.
   Плюс запасная память: последний адрес пиши в localStorage и, если человек
   пришёл на голый корень, предлагай вернуться туда, где он был.

2. ЗАСТАВКА. Владелец: «у яндекса когда открываешь приложение или сайт выходит
   чтобы красиво четко и пропадает тоже постепенно но быстро красиво главное.
   И главное чтоб постоянно не надоедало при перезагрузке сайта».
   Значит: жёлтый экран со знаком (IMG_6080.png — посмотри), показывается пока
   грузятся шрифты и первый экран, уходит плавно за 250-350 мс. Показываем
   ПОЛНОСТЬЮ только при холодном запуске (установленное приложение или первый
   заход за час) — при обычной перезагрузке она мелькает не дольше, чем
   загружается страница, и если всё пришло мгновенно, её не видно вовсе.
   Разметку заставки положи прямо в html, чтобы она была видна ДО загрузки
   скриптов, а гасил её shell.js.

3. КНОПКА «СКАЧАТЬ». Владелец: «для клиента браузера сделай кнопку сверху
   иконка название и тд кнопка скачать для андроид и айфон пва но клиенту типа
   приложение чтобы скачать кнопка делала вещи кнопка домой и тд чтобы много
   чего клиент не нажимал лишь Скачать и все удобно».
   Сделай сверху аккуратную полосу: значок сервиса, название, кнопка «Установить».
   - Android/Chrome: ловим beforeinstallprompt и ставим по-настоящему, одним тапом.
   - iPhone/Safari: своего диалога нет, поэтому показываем короткую шторку с
     двумя шагами и нарисованными значками: «Поделиться» → «На экран Домой».
   - Уже установлено (display-mode: standalone) — полосы нет вовсе.
   - Закрыл — не показываем неделю. Никакого приставания.
   Манифест клиента: имя «Sprinter Go», start_url «/», standalone, иконки уже
   лежат в web/, тема светлая. Служебный воркер — по образцу sw-courier.js,
   который уже есть: оболочка в кэше, /api НИКОГДА не кэшируется.

4. СВЕТЛАЯ ТЕМА ПО УМОЛЧАНИЮ: в html убери data-theme="dark", поправь
   meta theme-color, и встроенный скрипт выбора темы (он там есть) должен
   по умолчанию давать светлую, а не системную.

Проверь в браузере: заставка появляется и уходит, полоса установки видна на
Android-агенте и не видна в standalone, адрес меняется при открытии наложения
и восстанавливается после перезагрузки.`,
  },
]

const shell = await parallel(SHELL.map(m => () => agent(m.prompt, { label: m.label, phase: 'Оболочка' })))
log('Оболочка: ' + shell.filter(Boolean).length + ' из ' + SHELL.length)

phase('Экраны')

const SCREENS = [
  {
    label: 'карта: баг при отдалении и фиолетовые зоны',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ:
  ${P}/web/assets/js/core/map.js
  ${P}/web/assets/css/map.css

1. БАГ, О КОТОРОМ ВЛАДЕЛЕЦ ГОВОРИТ ВТОРОЙ РАЗ: «по карте баг снова. Когда
   отдаляешь и двигаешься по карте». В прошлый раз чинили дыры при отдалении —
   стало лучше, но осталось: если ОТДАЛИТЬ, а потом ТАЩИТЬ карту, дыры
   возвращаются. Воспроизведи сам: прогон ${P}/tools/map_test.mjs умеет ходить
   на живой сервер и меряет покрытие сеткой точек; допиши в свою проверку
   «отдалить на 4-6 шагов, затем протащить далеко в сторону несколько раз
   подряд, в том числе броском с инерцией» и добейся нуля дыр.
   Скорее всего дело в том, что при отдалении подслой и рабочий уровень
   добираются только вокруг старого центра, а при быстром протаскивании
   область видимости уезжает быстрее, чем догружается. Проверь сам, не верь
   этой догадке на слово.

2. ФИОЛЕТОВЫЕ ЗОНЫ СПРОСА. Владелец: «Фиолетовую зону для курьеров сделай чтобы
   если не на линии даже показывало если заказов много и много то цвет все
   фиолетовее и тд».
   Сейчас зоны розовые и показываются только на линии. Нужно:
   - шкала по плотности заказов: мало — едва заметная сиреневая дымка,
     много — насыщенный фиолетовый; ступеней пять, границы мягкие;
   - зоны видно и когда курьер не на линии: это и есть причина выйти;
   - подпись у крупной зоны: сколько там заказов сейчас;
   - рисуй на своём слое, поверх плиток, но под маркерами, и так, чтобы при
     движении карты зоны ехали вместе с ней без дрожания.
   Данные приходят из GET /api/v1/courier/zones — посмотри, что там сейчас.

3. БЛИЗКИЙ РЕЖИМ ДЛЯ КУРЬЕРА. Владелец: «когда принят заказ у него без кнопок
   навигатор а лишь близкая карта где он и куда ехать миллисекунды обновление
   чтобы маршрут двигался».
   Добавь в движок режим следования: map.follow({lat,lng,heading}, opts) —
   карта держит машину в нижней трети экрана, поворачивается по ходу движения
   (если opts.rotate), масштаб близкий, всё двигается плавно между приходящими
   координатами, а не прыжками. Экран курьера позовёт это сам.
   Плавность обязана быть настоящей: между двумя точками GPS раз в 2-3 секунды
   рисуй промежуточные кадры, иначе машина будет скакать.
   Добавь map.setRouteProgress(fraction) — пройденная часть маршрута гасится,
   оставшаяся яркая.

Свои проверки допиши в ${P}/tools/map_test.mjs (это твой файл на время работы).`,
  },
  {
    label: 'клиент: заказ, одна кнопка от двери до двери',
    prompt: `${BASE}

${LOOK}

ТВОИ ФАЙЛЫ:
  ${P}/web/assets/js/client/order.js
  ${P}/web/assets/js/client/address.js
  ${P}/web/assets/css/client.css

1. ГЛАВНАЯ ПРАВКА. Владелец: «когда указал откуда куда кнопка От двери до двери
   одна кнопка а не две».
   Сейчас переключатель «от двери до двери» стоит у КАЖДОЙ точки — их две, и это
   его раздражает. Сделай ОДИН переключатель на весь заказ, в списке опций рядом
   с грузчиками, как на снимке IMG_6093.png. Включён — курьер поднимается к двери
   на обеих точках, цена считается за обе.
   Сервер это уже понимает: extras строкой {code:'door_to_door', qty:<число точек>}.
   Один переключатель — значит qty равно числу точек маршрута, когда включён, и
   строки нет, когда выключен. Флажок у точки при этом ставь всем точкам сразу:
   курьеру важно знать, что подниматься надо везде.

2. ДЕТАЛИ ТОЧЕК. Владелец: «Данные подъезд и тд первой точки и данные второй
   точки надо заполнить где необходимо если галочки нету то заполнять не надо».
   То есть: отдельного шага-анкеты быть не должно. Как в Яндексе (IMG_6093.png):
   у каждой строки адреса справа кнопка «Детали», она открывает короткую шторку
   с подъездом, квартирой, этажом, домофоном и комментарием. Ничего не
   обязательно. Заполненное показывается под адресом одной серой строкой.
   Отдельный шаг 'details' из порядка шагов убери — он лишний.

3. ЭКРАН ЗАКАЗА ПО СНИМКАМ. Карточка тарифа с картинкой и вместимостью,
   сегментный переключатель классов машин, строка «Грузчики» с «Нет / 1 / 2»,
   чипы «О доставке», «О грузчиках» с (i), которые открывают пояснительный
   экран (infoStory из core/ui.js — сосед его делает прямо сейчас, зови по имени),
   внизу жёлтая кнопка во всю ширину с ценой.
   Пользуйся segmented(), chip(), rowGroup() из core/ui.js.

4. ПОИСК АДРЕСА как на IMG_6105.png: совпавшая часть запроса выделена цветом,
   расстояние от человека справа, свои места («Дом», «Работа») сверху отдельной
   карточкой.

5. Анимации перехода между шагами: содержимое уезжает и приезжает, а не мигает.
   Цена меняется бегущими цифрами, а не скачком.`,
  },
  {
    label: 'клиент: профиль по полочкам и анимации',
    prompt: `${BASE}

${LOOK}

ТВОИ ФАЙЛЫ:
  ${P}/web/assets/js/client/app.js

1. ПРОФИЛЬ ПО ПОЛОЧКАМ. Владелец: «раздел профиля клиента там модальное окно
   внутри слишком длинное по полочкам удобно распредели чтобы сверху вниз не
   листать постоянно».
   Сейчас в шторке профиля всё подряд одной лентой. Разложи на разделы с
   переключением, а не прокруткой: сверху шапка с аватаром, именем и телефоном,
   под ней вкладки или крупные плитки — «Заказы», «Бонусы», «Адреса»,
   «Настройки». Каждый раздел помещается в экран без бесконечной прокрутки.
   Открытый раздел отражается в адресе через router.overlay() (сосед делает его
   в core/router.js прямо сейчас) — перезагрузил, вернулся туда же.

2. ПОЛНОЭКРАННЫЕ ДЕТАЛИ. Историю заказа, бонусы целиком и настройки открывай
   через sheet({ full: true }) — сосед добавляет этот режим в core/ui.js.

3. АНИМАЦИИ. Владелец: «Анимации сделай для клиента крутые чтобы ему было
   приятно нажимать что-то открывать».
   Что реально приятно и не мешает:
   - разделы въезжают сбоку, назад — в обратную сторону;
   - плитки при нажатии проседают и отпружинивают;
   - число бонусов при начислении добегает до нового значения;
   - список истории появляется строками с задержкой в 20-30 мс друг за другом,
     один раз при открытии, а не при каждой перерисовке;
   - переключение языка меняет текст с короткой растворяющейся сменой.
   Всё гасится при prefers-reduced-motion.

4. Кнопку установки приложения в профиль НЕ добавляй — её делает сосед в
   core/shell.js полосой сверху. Не дублируй.`,
  },
  {
    label: 'клиент: отслеживание, чат, игра за кнопкой',
    prompt: `${BASE}

${LOOK}

ТВОИ ФАЙЛЫ:
  ${P}/web/assets/js/client/track.js

1. ЧАТ ПРОГОНЯЕТ. Владелец: «Чат бывает прогоняет между клиентом и курьером».
   Это баг, найди его. Скорее всего одно из двух или оба:
   - список сообщений перерисовывается целиком на каждое событие, и прокрутка
     уезжает к началу или дёргается;
   - своё отправленное сообщение появляется дважды: сразу и потом из потока
     событий, потому что сверяются не по идентификатору.
   Почини так, чтобы: прокрутка стояла на месте, когда человек читает старое,
   и уезжала вниз только если он и так был внизу; своё сообщение появлялось
   мгновенно и не задваивалось; приходящие дорисовывались снизу по одному.

2. ПОЛНОЭКРАННЫЕ ДЕТАЛИ ЗАКАЗА через sheet({ full: true }) — «а то листать
   приходится неудобно».

3. ИГРА ТОЛЬКО ПО КНОПКЕ. Владелец: «сделай чтобы игра если модальное окно
   свернута не появлялась постоянно раздражает чтобы кнопка Игра и все и чтоб
   не мешало». Сейчас игра лезет сама, когда панель свёрнута. Убери это
   насовсем: на экране поиска машины одна скромная кнопка «Поиграть», игра
   открывается только по ней и закрывается крестиком. Свернул панель — игры нет.

4. ЖИВАЯ КАРТА ПОДАЧИ. Машина едет плавно (движок это умеет, сосед добавляет
   map.follow и промежуточные кадры), время подачи тикает, маршрут гаснет
   позади машины.

5. Наложения (чат, детали, оценка) отражай в адресе через router.overlay().`,
  },
  {
    label: 'курьер: карта с маршрутом сразу и навигация',
    prompt: `${BASE}

${LOOK}

ТВОИ ФАЙЛЫ:
  ${P}/web/assets/js/courier/work.js
  ${P}/web/assets/js/courier/app.js
  ${P}/web/assets/css/courier.css

ВЛАДЕЛЕЦ СКАЗАЛ ДОСЛОВНО: «когда курьер принимает заказ там сразу карта
показывается с маршрутом и когда принят заказ у него без кнопок навигатор а
лишь близкая карта где он и куда ехать миллисекунды обновление чтобы маршрут
двигался и он добрался до точки а потом к б ну ты понял все как часы должно
работать».

1. ПРИНЯЛ — СРАЗУ КАРТА С МАРШРУТОМ. Не список адресов, не карточка: маршрут от
   машины до точки А нарисован, карта подогнана под него, панель свёрнута до
   одной строки. Без нажатий.

2. СВОЙ НАВИГАТОР ВНУТРИ. Близкий вид, машина в нижней трети, карта
   поворачивается по ходу, пройденное гаснет. Зови map.follow() и
   map.setRouteProgress() — сосед добавляет их в core/map.js прямо сейчас.
   Обновление плавное: между точками GPS рисуются промежуточные кадры.
   Кнопка «Открыть в Яндекс Навигаторе» пусть останется, но маленькой и сбоку —
   владелец хочет, чтобы своей карты хватало.
   Доехал до А — маршрут сам перестраивается на Б. «Как часы» — это про то, что
   человеку не надо ничего нажимать, чтобы маршрут обновился.

3. ПОЛНОЭКРАННЫЕ ДЕТАЛИ заказа через sheet({ full: true }): что везём, адреса
   целиком, телефон, чат, деньги. Одним движением, без прокрутки мелкого текста.

4. ФИОЛЕТОВЫЕ ЗОНЫ показывай и когда курьер НЕ на линии — движок это умеет
   (сосед делает шкалу). На экране смены под кнопкой «Выйти на линию» строка
   вроде «Рядом с вами сейчас 12 заказов» — это и есть причина выйти.

5. Полоса установки приложения и заставка — не твоё, их делает сосед.`,
  },
  {
    label: 'админка: демо-оплата, разделы, полный экран',
    prompt: `${BASE}

${LOOK}

ТВОИ ФАЙЛЫ:
  ${P}/web/assets/js/admin/pages.js
  ${P}/web/assets/js/admin/forms.js
  ${P}/web/assets/js/admin/app.js
  ${P}/web/assets/css/admin.css

1. ДЕМО-ОПЛАТА. Владелец: «в админке демо оплата чтобы демо кр код и тд выходил
   я сам все видел и тд». Он хочет увидеть экран оплаты своими глазами, не
   имея договора с банком.
   Сервер получит режим payment.demo (я, интегратор, делаю его сейчас):
   при включённом демо POST /pay/{pid}/qr выпускает настоящий QR-код, нарисованный
   самим сервисом, и оплату можно подтвердить кнопкой. В разделе «Оплата»
   добавь: переключатель «Демо-режим», под ним объяснение «Клиент увидит
   настоящий экран оплаты, но деньги никуда не уйдут», и кнопку «Показать
   пример» — она выпускает демо-заказ и показывает тот самый QR прямо в админке,
   в шторке, вместе с кнопкой «Отметить оплаченным».
   Эндпоинты: POST /api/v1/admin/pay/demo — создать пример, вернёт
   {public_id, token, qr_base64, amount}; POST /api/v1/admin/pay/demo/confirm
   с {public_id} — отметить оплаченным.

2. ЗАПОМИНАНИЕ РАЗДЕЛА. Адрес уже хеш-адресный (#/orders и так далее), но
   открытая карточка заказа или курьера в адрес не попадает. Отрази их тоже:
   #/orders/123. Перезагрузил — та же карточка открыта.

3. ПОЛНОЭКРАННЫЕ КАРТОЧКИ. Заказ, курьер, клиент — через sheet({ full: true }),
   сосед добавляет режим в core/ui.js.

4. СВЕТЛАЯ ТЕМА. Админка сейчас жёстко тёмная (data-theme="dark" в admin.html —
   его правит сосед). Убедись, что в светлой теме читаемо всё: графики, таблицы,
   чипы статусов, ползунки. Это твоя часть работы, не соседа.

5. ЖИВОЕ НАЖАТИЕ и аккуратные переходы между разделами — пользуйся pressable()
   из core/ui.js.`,
  },
]

const screens = await parallel(SCREENS.map(m => () => agent(m.prompt, { label: m.label, phase: 'Экраны' })))
log('Экраны: ' + screens.filter(Boolean).length + ' из ' + SCREENS.length)

phase('Своё')

const OWN = [
  {
    label: 'свой QR-кодер и живая ссылка на заказ',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ:
  ${P}/server/qrcode.py            (создать)
  ${P}/server/share.py             (править)
  ${P}/web/assets/js/share/watch.js (создать)
  ${P}/web/assets/css/share.css     (создать)

1. СВОЙ QR-КОДЕР, server/qrcode.py. Чистый Python, без библиотек.
   Нужен для демо-оплаты (показать владельцу настоящий код без банка) и как
   запасной вариант, если банк пришлёт только ссылку без картинки.
   Реализуй честно: байтовый режим, уровень коррекции M, версии с 1 по 10
   (этого хватает на ссылку банка), маскирование с выбором лучшей маски по
   штрафам, полиномы Рида-Соломона. Не подделка и не заглушка — код должен
   читаться настоящим телефоном.
   API: encode(text) -> матрица булевых; png(text, size=512, border=4) -> bytes
   готового PNG (PNG собирай вручную через zlib, как это уже сделано в share.py —
   посмотри, там есть рабочий сборщик).
   Обязательно проверь себя: раскодируй свою же матрицу обратно хотя бы для
   нескольких строк разной длины, или сверь контрольные данные из стандарта.
   Напиши проверку в ${P}/tools/qr_test.py — она твоя.

2. ЖИВАЯ ССЫЛКА НА ЗАКАЗ. Владелец: «когда поделиться делаешь заказом чтобы
   карта и тд обновление движений и тд все данные пусть будут лишь кнопку
   отменить убери чтоб кто попало не смог отменять».
   Я, интегратор, прямо сейчас делаю на сервере отдельный токен только для
   просмотра. Контракт, на него и пиши:
   - у заказа появляется view_token (24 символа), он отличается от track_token;
   - GET /api/v1/orders/{pid}?t=<view_token> отдаёт заказ БЕЗ телефонов, квартир,
     домофонов и имени клиента, и с полем "readonly": true;
   - GET /api/v1/orders/{pid}/stream?t=<view_token> — тот же поток событий,
     так же обрезанный;
   - любое действие (отмена, оценка, сообщения) с этим токеном — 403.
   Страница /share/{pid}?v=<view_token> теперь должна быть ЖИВОЙ: карта с
   машиной, маршрут, статус, время. Кнопки «Отменить» на ней нет и быть не может.
   Разметку и мета-теги Open Graph оставь как есть — они работают, робот
   мессенджера приходит без токена и видит прежнюю карточку. А когда токен
   есть и это живой человек, страница подключает web/assets/js/share/watch.js:
   он берёт движок карты из ../core/map.js (ЧУЖОЙ ФАЙЛ, только импортируй) и
   показывает движение.
   Скажи в share.py, что share_url(order) теперь возвращает путь с ?v=токеном.`,
  },
  {
    label: 'приложение для Android: своё, без чужих библиотек',
    prompt: `${BASE}

ТВОЯ ПАПКА (создать целиком):
  ${P}/../android/          — то есть рядом с platform, в корне проекта

ВЛАДЕЛЕЦ: «дай апк для водителей четкое приложение фоновое обновление гео
уведомления пуш четко сделай самописный».

В этой машине УЖЕ СТОИТ Android SDK: ANDROID_HOME=/opt/android-sdk,
platforms;android-34, build-tools;34.0.0, Java 21, Gradle в /opt/gradle/bin/gradle.
Значит собрать APK можно прямо здесь — сделай так, чтобы собиралось.

ЧТО ЭТО ЗА ПРИЛОЖЕНИЕ. Оболочка вокруг веб-приложения курьера плюс то, чего
браузер не умеет:
1. WebView на https://sprintergo.kg/go/courier — с включённым JS, геолокацией,
   своим User-Agent, чтобы веб знал, что он внутри приложения.
2. СЛУЖБА НА ПЕРЕДНЕМ ПЛАНЕ, которая живёт, пока курьер на линии:
   - берёт координаты раз в 5-10 секунд (чаще при движении, реже на стоянке) и
     шлёт POST /api/v1/courier/geo с токеном курьера;
   - держит открытым поток событий GET /api/v1/courier/stream (это обычный SSE
     поверх HTTP — читай построчно, никаких библиотек) и на событие offer
     показывает уведомление со звуком и вибрацией: адрес, расстояние, цена;
   - переживает засыпание экрана: постоянное уведомление «Sprinter Go — на линии»,
     wake lock, перезапуск при обрыве связи с выдержкой.
   Никакого Firebase: уведомления показываем сами, поток держим сами. Так и
   надёжнее, и не нужен ни аккаунт Google, ни ключи.
3. Токен курьера приложение берёт из WebView (JavaScript-мост) после входа и
   хранит в EncryptedSharedPreferences или, если без зависимостей, в обычных,
   но не в открытом виде.
4. Разрешения по-человечески: сначала объясняем зачем, потом просим. Фоновая
   геолокация — отдельным шагом, как требует Android 11+.
5. Иконка и заставка в фирменных цветах: жёлтый #FFDF00, чёрный знак.
   Иконку нарисуй векторным drawable, картинки не тащи.

ТРЕБОВАНИЯ К СБОРКЕ:
- Gradle Kotlin или Groovy — на твой выбор, но БЕЗ интернета за плагинами сверх
  того, что есть на dl.google.com и Maven Central (они доступны).
- Язык Java (не Kotlin: меньше зависимостей в сборке).
- minSdk 24, targetSdk 34.
- Собери сам: ANDROID_HOME=/opt/android-sdk /opt/gradle/bin/gradle assembleDebug
  в своей папке. Добейся, чтобы APK реально собрался. Путь к нему верни в отчёте.
- Подпись: сгенерируй ключ через keytool (пароль напиши в отчёте, положи
  keystore в android/keystore/), собери и assembleRelease тоже.
- README.md в android/ по-русски: как собрать, как поставить на телефон, как
  обновлять.

ВАЖНО: ничего в ${P}/web и ${P}/server не трогай. Если вебу нужен мост — опиши
в отчёте, что именно должен позвать веб, я это добавлю сам.`,
  },
]

const own = await parallel(OWN.map(m => () => agent(m.prompt, { label: m.label, phase: 'Своё' })))
log('Своё: ' + own.filter(Boolean).length + ' из ' + OWN.length)

return {
  оболочка: shell.filter(Boolean).length,
  экраны: screens.filter(Boolean).length,
  своё: own.filter(Boolean).length,
}
