export const meta = {
  name: 'sprintergo-v2',
  description: 'Доработка платформы: чат, верификация, профиль, зоны спроса, жесты, звук, анимации',
  phases: [
    { title: 'Сервер', detail: 'чат, верификация, зоны, профиль клиента, пробки' },
    { title: 'Экраны', detail: 'клиент, курьер, карта' },
  ],
}

const P = '/tmp/claude-0/-home-user-Lux/7dca5838-9194-5f06-a843-0c83e0bebcb1/scratchpad/platform'

const BASE = `Проект: сервис грузоперевозок Sprinter Go, Бишкек. Работает на sprintergo.kg/go/.
Прочитай ${P}/SPEC.md — это контракт. Прочитай файлы, которые правишь, целиком.

ЖЕЛЕЗНЫЕ ПРАВИЛА:
- Ноль внешних зависимостей: Python только стандартная библиотека, JS только нативные
  ES-модули, никаких библиотек, сборщиков и CDN. CSS руками.
- Комментарии и текст интерфейса по-русски, живым языком. Имена в коде латиницей.
- Деньги целые тыйыны, время целые секунды unix UTC.
- Мобильный первым: 360-430 px. Кнопки не мельче 44 px.
- Пиши ТОЛЬКО свои файлы. Чужие не трогай — их правят параллельно.
- Никаких TODO и заглушек.
- Проверь синтаксис сам: python3 -m py_compile для .py,
  node --input-type=module --check < файл для .js.

СТРОКИ ИНТЕРФЕЙСА: не лезь в общий словарь lang.ru.js — его правят другие.
Свои строки объявляй прямо в своём модуле и подключай так:
    import { t, extend } from '../core/i18n.js';
    extend({ ru: { 'chat.title': 'Чат с курьером' }, ky: { 'chat.title': 'Курьер менен чат' } });
Кыргызский пиши как говорят в Бишкеке, а не машинным переводом: «жүк ташуу», «жүкчү»,
«кайдан/кайда», «баасы», «унаа», «кабат», слово «заказ» и «мебель» так и остаются.

В конце верни короткий отчёт: что сделал, какие файлы, что ждёшь от соседних модулей.`

phase('Сервер')

const SERVER = [
  {
    label: 'сервер: чат, профиль, верификация, зоны',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ (создать):
  ${P}/server/routers/extra.py
  ${P}/server/uploads.py

Схема базы уже готова (смотри ${P}/server/db.py): таблица messages, у couriers появились
verify_status/verify_photo/verify_note/verified_at/photo, у clients — token и name_asked.
Ничего в db.py не меняй.

Сделай модуль extra.py с функцией register(app) — как в соседних роутерах, и объектом router.

1. ЧАТ между клиентом и курьером по заказу.
   GET  /orders/{pid}/messages?t=токен        — история для клиента
   POST /orders/{pid}/messages?t=токен        — {text}, отправка от клиента
   GET  /courier/orders/{id}/messages         — история для курьера (по Bearer)
   POST /courier/orders/{id}/messages         — отправка от курьера
   Каждое сообщение публикуй в HUB: тема order:{public_id} и courier:{user_id},
   событие 'message'. Отметка прочтения: POST .../messages/read.
   Ограничения: текст до 1000 символов, не больше 30 сообщений в минуту с одной стороны,
   чат живёт пока заказ не завершён более суток назад. Экранирования на сервере не делай —
   отдавай как есть, экранирует фронт, но проверь длину и обрежь управляющие символы.

2. ПРОФИЛЬ КЛИЕНТА без регистрации.
   Клиент опознаётся токеном: при создании заказа сервер выдаёт clients.token
   (32 случайных байта, hex), фронт хранит его у себя и присылает дальше.
   GET   /client/profile?token=   — имя, телефон, количество заказов, рейтинг
   PATCH /client/profile?token=   — {name}
   GET   /client/orders?token=    — история заказов, постранично, свежие сверху,
         с адресами, датой, суммой, статусом и оценкой
   POST  /client/claim            — {phone, order_id, track_token} выдаёт токен тому,
         кто доказал владение заказом. Токен не выдавать по одному телефону.

3. ВЕРИФИКАЦИЯ КУРЬЕРА.
   POST /courier/verify — {photo: "data:image/jpeg;base64,..."}.
   Сохраняй файл через uploads.py, в базу пиши только имя файла.
   Ставь verify_status='pending'. Повторная отправка после отказа разрешена.
   GET /courier/verify — текущий статус и причина отказа.
   GET /uploads/{name} — отдача файла. ВАЖНО: фото паспорта видит только сам курьер
   и админ. Проверяй права, имя файла проверяй на выход за каталог.

4. ЗОНЫ ПОВЫШЕННОГО СПРОСА для карты курьера.
   GET /courier/zones — по заказам за последние 3 часа считаешь плотность по сетке
   примерно 700 м, возвращаешь ячейки с уровнем 0..1 и центром:
   {cells:[{lat,lng,level,orders}], updated_at}. Пустой список — нормальный ответ.
   Считай не чаще раза в минуту, результат держи в памяти.

uploads.py — сохранение картинок из data URL: разбор заголовка, проверка что это
jpeg/png/webp, ограничение 6 МБ, уменьшение размера НЕ делай (это забота фронта),
случайное имя, каталог из SG_DATA/uploads, функция read(name) с защитой от выхода
за каталог. Плюс remove(name) и человекочитаемые ошибки.`,
  },
  {
    label: 'сервер: допуск к заказам, оценка клиента, пробки',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ (править существующие):
  ${P}/server/dispatch.py
  ${P}/server/routers/courier.py
  ${P}/server/routers/admin.py
  ${P}/server/geo.py

1. ДОПУСК К ЗАКАЗАМ ТОЛЬКО ПОСЛЕ ПРОВЕРКИ.
   В dispatch.candidates добавь условие: couriers.verify_status='approved'.
   Непроверенный курьер может войти, выйти на линию и видеть интерфейс,
   но заказов не получает. Причину он должен понимать: в /courier/state
   (или что там отдаёт состояние) верни verify_status и текст причины.

2. КУРЬЕР ОЦЕНИВАЕТ КЛИЕНТА.
   POST /courier/orders/{id}/rate-client — {rating: 1..5, comment}.
   Пишет в orders.courier_rating и courier_comment, обновляет
   clients.rating_sum и rating_count. Только по своему завершённому заказу,
   только один раз. В карточке заказа отдавай курьеру рейтинг клиента,
   чтобы он видел, с кем едет.

3. АДМИНКА: МОДЕРАЦИЯ ВЕРИФИКАЦИИ.
   GET   /admin/verify            — очередь на проверку: курьер, машина, дата, фото
   PATCH /admin/verify/{user_id}  — {status: 'approved'|'rejected', note}
   При решении шли письмо (mailer уже есть): одобрен или отказ с причиной.
   В общем списке /admin/couriers добавь поле verify_status, чтобы было видно сразу.

4. ПРОБКИ И ЧЕСТНОЕ ВРЕМЯ в geo.py.
   Сейчас время считается по средней скорости. Сделай честнее:
   - если в настройках есть ключ маршрутизатора Яндекса (route.provider='yandex',
     route.key), используй его и бери время с пробками;
   - иначе OSRM как сейчас, но время умножай на коэффициент часа пик Бишкека:
     будни 8:00-10:00 и 17:00-20:00 — заметно дольше, ночь — быстрее.
     Коэффициенты вынеси в настройки route.rush_factor и route.night_factor
     со значениями по умолчанию, чтобы владелец мог подкрутить.
   - возвращай оба значения: duration_s (без пробок) и duration_traffic_s.
   Часовой пояс Бишкека брать из настроек service.tz, не из системного времени.`,
  },
]

const srv = await parallel(SERVER.map(m => () => agent(m.prompt, { label: m.label, phase: 'Сервер' })))
log('Сервер готов: ' + srv.filter(Boolean).length + ' из ' + SERVER.length)

phase('Экраны')

const SCREENS = [
  {
    label: 'клиент: жесты, шторки, запрет зума',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ (править):
  ${P}/web/index.html
  ${P}/web/assets/css/client.css
  ${P}/web/assets/js/core/ui.js

Заказчик жалуется: шторки резкие, на айфоне сайт зумится от двойного тапа и при
попадании в поле ввода, карта закрыта панелью.

1. ШТОРКА КАК В ЯНДЕКС GO. Панель снизу живёт в трёх положениях: развёрнута,
   наполовину, свёрнута до заголовка. Тянется пальцем за грип, кидок вниз сворачивает,
   вверх разворачивает — с инерцией, а не ступенькой. Пружинная кривая --ease-spring,
   длительность --dur-2. Пока палец на панели, страница под ней не скроллится.
   КОГДА ЧЕЛОВЕК ТРОГАЕТ КАРТУ — панель сама уезжает вниз до половины, чтобы не мешать,
   и возвращается, когда он отпустил и не двигал карту секунду.

2. НИКАКОГО ЗУМА СТРАНИЦЫ. Полностью убрать:
   - двойной тап по любому элементу интерфейса,
   - щипок по интерфейсу (на самой карте щипок должен работать),
   - автозум айфона при фокусе в поле — единственное надёжное средство это
     font-size не меньше 16px у ВСЕХ input, select и textarea, сделай это правилом в CSS;
   - в мета-теге viewport добавь maximum-scale=1 и user-scalable=no, но помни,
     что Safari его игнорирует — поэтому нужны и touch-action: manipulation
     на интерактивных элементах, и обработчик, гасящий второй тап в пределах 300 мс.
   Проверь, что на самой карте при этом остаются и щипковый зум, и двойной тап для зума.

3. АНИМАЦИИ. Переходы между шагами: содержимое сдвигается и растворяется, а не мигает.
   Кнопки отзываются нажатием. Появление списков — лёгким каскадом. Всё через
   токены длительностей, всё уважает prefers-reduced-motion.

4. В ui.js добавь два общих средства, ими будут пользоваться соседние модули:
   - photoViewer(src, {alt}) — фото почти на весь экран поверх всего, затемнение,
     закрытие по крестику, тапу мимо, Esc и свайпу вниз, плавное появление;
   - copyText(text, okMessage) — копирование в буфер с запасным путём через
     скрытое поле для старых браузеров, показывает тост.
   Ничего другого в ui.js не ломай: им пользуются все три приложения.`,
  },
  {
    label: 'клиент: профиль, память, история',
    prompt: `${BASE}

ТВОЙ ФАЙЛ (править):
  ${P}/web/assets/js/client/app.js

Сервер уже умеет (модуль ${P}/server/routers/extra.py, прочитай его):
  GET/PATCH /client/profile?token=, GET /client/orders?token=, POST /client/claim.
Токен клиента приходит при создании заказа.

1. ПОМНИМ ЧЕЛОВЕКА. Телефон, имя и токен сохраняем в localStorage под ключом sg_client.
   Если данные есть — при следующем заказе поля уже заполнены и второй раз их
   не спрашиваем: сразу кнопка «Заказать». Ошибётся телефоном — сможет поправить.

2. КРУГЛАЯ КНОПКА ПРОФИЛЯ справа сверху, рядом с переключателем языка.
   Если человек новый — её нет. Если есть данные — кружок с инициалами или
   фотографией, при нажатии открывается профиль.

3. ЭКРАН ПРОФИЛЯ: имя (можно поменять), телефон, количество заказов, рейтинг звёздами,
   переключатель языка, переключатель темы, кнопка «Забыть меня» с подтверждением,
   ссылка на поддержку в WhatsApp.

4. ИСТОРИЯ ЗАКАЗОВ: список от свежих к старым. В строке — адреса, дата, сумма,
   статус цветной меткой, оценка. Тап открывает подробности: маршрут на карте,
   разбивка цены, курьер, кнопка «Повторить заказ», которая подставляет те же адреса
   в новый заказ. Постраничная догрузка при прокрутке, состояния загрузки и пустого списка.

Соседние модули: шторки и запрет зума делает другой агент в index.html и client.css,
экран отслеживания и оценка — третий в track.js, выбор машины — четвёртый в order.js.
Их файлы не трогай, при необходимости экспортируй из app.js то, что им понадобится.`,
  },
  {
    label: 'клиент: ожидание, оценка, чат',
    prompt: `${BASE}

ТВОЙ ФАЙЛ (править):
  ${P}/web/assets/js/client/track.js

1. ПОКА ИЩЕТСЯ МАШИНА — не пустой экран. Сделай на карте маленькую игру, чтобы
   ожидание не раздражало: простую, понятную с первого взгляда, управляемую одним
   пальцем, без правил и обучения. Например, коробки падают сверху, их надо ловить
   в кузов, за каждую очко, рекорд сохраняется. Игра не мешает: она в свёрнутом
   состоянии панели, поверх карты, и мгновенно исчезает, как только курьер найден.
   Никаких библиотек — canvas или обычные элементы, requestAnimationFrame.
   Работает на слабом телефоне и не жрёт батарею: останавливается, когда вкладка в фоне.

2. ЭКРАН ОЦЕНКИ С ЭМОЦИЕЙ. Пять звёзд, при выборе:
   1-3 звезды — «Нам очень жаль» + грустная анимация, обязательное поле «что пошло не так»
   с готовыми вариантами (опоздал, повредили груз, нагрубил, дороже договорённого, другое);
   4-5 звёзд — «Спасибо, мы рады!» + радостная анимация, конфетти или прыгающие смайлы,
   поле для отзыва по желанию, предложение сохранить курьера в избранные.
   Анимации рисуй сам — эмодзи и CSS, без картинок и библиотек.

3. КАРТОЧКА КУРЬЕРА:
   - тап по госномеру копирует его (используй copyText из core/ui.js) и показывает тост;
   - тап по аватарке открывает фото почти на весь экран (photoViewer из core/ui.js);
   - кнопка чата рядом с «Позвонить».

4. ЧАТ С КУРЬЕРОМ. Сервер готов, читай ${P}/server/routers/extra.py:
   GET и POST /orders/{pid}/messages?t=токен, живые сообщения приходят событием
   'message' в том же потоке SSE, что и обновления заказа.
   Экран чата: пузыри, время, отметка прочтения, автопрокрутка вниз, поле ввода
   не перекрывается клавиатурой, счётчик непрочитанных на кнопке.
   ОБЯЗАТЕЛЬНО экранируй текст сообщений: никакого innerHTML для чужого текста.
   Рядом кнопка «Написать в WhatsApp» на случай, если человеку так привычнее.`,
  },
  {
    label: 'клиент: реалистичные машины и честное время',
    prompt: `${BASE}

ТВОЙ ФАЙЛ (править):
  ${P}/web/assets/js/client/order.js

1. КАРТОЧКИ МАШИН ДОЛЖНЫ ВЫГЛЯДЕТЬ ЖИВЫМИ. Сейчас там простые значки.
   Нарисуй для каждого класса машины аккуратную иллюстрацию в SVG, рисуй сам,
   никаких картинок и библиотек:
     express   — легковая с прицепом или пикап
     van       — Mercedes Sprinter, узнаваемый силуэт фургона
     truck     — грузовик с тентом на 3 тонны
     truck_big — большой грузовик на 5 тонн
   Уровень: чистые формы, два-три цвета из токенов, лёгкая тень, узнаётся с первого
   взгляда на маленьком экране. Выбранная карточка слегка приподнимается и подсвечивается.
   Под названием — вместимость и габариты кузова, чтобы человек понимал, что влезет.

2. ЧЕСТНОЕ ВРЕМЯ И РАССТОЯНИЕ. Сервер теперь отдаёт duration_traffic_s (с пробками)
   вместе с duration_s — читай ${P}/server/geo.py, там это сделано.
   Показывай время с пробками и помечай это словом, чтобы человек понимал,
   почему дольше. Расстояние — с одним знаком после запятой.

3. Пересчёт цены при смене тарифа, числа грузчиков и допуслуг — мгновенный,
   с плавной сменой цифры, а не рывком. Запрос с задержкой, предыдущий отменяется.
   Пока считается — не мигать пустотой, показывать прежнее значение приглушённым.`,
  },
  {
    label: 'курьер: вёрстка, футер, иконки',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ (править):
  ${P}/web/courier.html
  ${P}/web/assets/css/courier.css

Заказчик жалуется, что в профиле мало иконок и нет нижней навигации.

1. НИЖНЯЯ НАВИГАЦИЯ на четыре раздела: Смена, Заказы, Заработок, Профиль.
   Иконки рисуй в SVG сам, ровные, одной толщины линий, активный раздел жёлтый.
   Панель прижата к низу, учитывает --safe-b, не перекрывает содержимое,
   прячется, когда открыт активный заказ на весь экран.

2. ПРОФИЛЬ С ИКОНКАМИ: строки с иконкой слева — данные, машина, документы и проверка,
   рейтинг, язык, тема, поддержка, выход. Каждая строка нажимается целиком.
   Сверху крупная карточка: фото, имя, рейтинг звёздами, «на линии с такого-то года»,
   значок «Проверен» зелёной галочкой, если проверка пройдена.

3. Те же правила, что и у клиента: поля ввода не мельче 16px, чтобы айфон не зумил;
   touch-action: manipulation; никакого зума по двойному тапу.

4. Экран активного заказа должен уметь занять весь экран поверх навигации,
   с картой сверху и панелью снизу — размести классы под это, логику пишет другой агент.`,
  },
  {
    label: 'курьер: карта заказа, зоны, звук, оценка',
    prompt: `${BASE}

ТВОЙ ФАЙЛ (править):
  ${P}/web/assets/js/courier/work.js

1. КАРТА В АКТИВНОМ ЗАКАЗЕ. Сейчас курьер видит только адреса — этого мало.
   Покажи карту с маршрутом: своя позиция, точка погрузки, точка выгрузки, линия
   маршрута, оставшееся расстояние и время. Карта сверху, панель заказа снизу,
   панель тянется пальцем и сворачивается, чтобы открыть карту целиком.
   Кнопки «Открыть в Яндекс Навигаторе» и «Открыть в 2ГИС» — ссылки вида
   yandexnavi://build_route_on_map?lat_to=..&lon_to=.. и dgis://2gis.ru/routeSearch/rsType/car/to/lon,lat
   с запасным переходом на веб-версию, если приложение не установлено.

2. ЗОНЫ ПОВЫШЕННОГО СПРОСА, как розовые пятна в Яндексе. Сервер отдаёт
   GET /courier/zones — читай ${P}/server/routers/extra.py.
   Рисуй ячейки полупрозрачным розовым, насыщенность по уровню спроса, мягкие
   границы, плавное появление. Обновляй раз в минуту, только когда курьер на линии
   и не в заказе. Подпись-подсказка: «здесь сейчас больше заказов».

3. ЗВУК НОВОГО ЗАКАЗА. Приятный и узнаваемый, чтобы водитель услышал за рулём,
   но не вздрогнул. Синтезируй через Web Audio, файлов не подключай: два-три
   мягких тона по возрастанию, короткая реверберация, повтор трижды с паузой,
   пока предложение живо. Громкость и включение — в настройках профиля, помни выбор.
   Учти, что браузер не даст звучать до первого касания экрана: разблокируй
   аудиоконтекст на первом взаимодействии и честно покажи, если звук пока запрещён.
   Вместе со звуком — вибрация, если телефон умеет.

4. ОЦЕНКА КЛИЕНТА после завершения заказа: пять звёзд и короткий комментарий,
   отправка на POST /courier/orders/{id}/rate-client. Пропустить можно.
   В карточке заказа показывай рейтинг клиента, чтобы курьер видел, с кем едет.

5. ЧАТ С КЛИЕНТОМ: GET и POST /courier/orders/{id}/messages, живые сообщения
   событием 'message' в потоке курьера. Пузыри, непрочитанные, автопрокрутка.
   Чужой текст только через textContent, никакого innerHTML.`,
  },
  {
    label: 'курьер: верификация и профиль',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ (править):
  ${P}/web/assets/js/courier/auth.js
  ${P}/web/assets/js/courier/app.js

1. ВЕРИФИКАЦИЯ ПО ФОТО С ПАСПОРТОМ — без неё заказов не будет.
   Сервер: POST /courier/verify {photo}, GET /courier/verify — читай
   ${P}/server/routers/extra.py.
   Экран проверки: понятное объяснение зачем это, образец правильного снимка
   (нарисуй схемой, не фотографией), кнопка «Сделать фото» — открывает камеру
   через input type=file capture=user, и «Выбрать из галереи».
   ОБЯЗАТЕЛЬНО ужми снимок перед отправкой: canvas, длинная сторона до 1400 px,
   JPEG качеством 0.82 — иначе телефонное фото на 5 МБ не дойдёт.
   Покажи, что получилось, дай переснять, потом отправка с индикатором.
   После отправки — экран «Проверяем, обычно занимает до суток», статус обновляется.
   При отказе — причина от админа и кнопка «Отправить заново».

2. ДОСТУП К ЗАКАЗАМ. Пока проверка не пройдена, переключатель «На линии»
   заблокирован, и вместо него объяснение с кнопкой перехода к проверке.
   Не прячь остальной интерфейс: пусть человек видит, что его ждёт.

3. ПРОФИЛЬ: разделы данных, машины, документов, рейтинга, языка, темы, звука,
   поддержки и выхода. Аватар можно поставить — тем же способом со сжатием,
   отправляется как photo. Тап по аватару открывает его на весь экран
   (photoViewer из core/ui.js).

4. Нижняя навигация из четырёх разделов свёрстана другим агентом в courier.html
   и courier.css — подключи логику переключения к её кнопкам, разметку не меняй.`,
  },
  {
    label: 'карта: чёткость, плавность, машина, зоны',
    prompt: `${BASE}

ТВОИ ФАЙЛЫ (править):
  ${P}/web/assets/js/core/map.js
  ${P}/web/assets/css/map.css

Заказчик говорит: «карта плавает, прогоняет, сделай чтобы всё было чётким».

1. ЧЁТКОСТЬ НА РЕТИНЕ. Сейчас на экране с плотностью 3 запрашивается обычная плитка
   и картинка мылит. Запрашивай удвоенные плитки, когда devicePixelRatio больше 1.4:
   у OSM и Carto это суффикс @2x (плейсхолдер {r} уже есть), у Яндекса —
   параметр scale=2, у 2ГИС свой. Добавь плейсхолдер {scale}, который даёт 1 или 2,
   чтобы шаблон из настроек мог его использовать. Если удвоенная плитка не пришла,
   молча откатывайся на обычную, а не показывай дыру.

2. НИКАКОГО ДРОЖАНИЯ. Плитки и маркеры позиционируй по целым пикселям устройства
   (округляй смещение с учётом devicePixelRatio), иначе на дробных значениях
   всё подрагивает и мылится. Слой плиток двигай одним трансформом контейнера,
   а не пересчётом каждой плитки. will-change и backface-visibility там, где помогает,
   но не на всём подряд.

3. МАШИНА КУРЬЕРА. Сейчас простой значок. Нарисуй в SVG вид сверху: узнаваемый
   фургон, жёлтый корпус, тёмное лобовое, лёгкая тень под машиной. Плавный поворот
   по направлению движения — доворачивай по кратчайшей дуге, а не через полный круг.
   Движение между точками сглаживай: не прыжок, а проезд за время между обновлениями.
   Когда машина стоит — лёгкое дыхание тени, чтобы было видно, что связь живая.

4. ЗОНЫ СПРОСА как примитив карты: map.zones(cells, {color}) рисует полупрозрачные
   пятна с мягкими краями, setCells обновляет, remove убирает. Рисуй на canvas
   поверх плиток, но под маркерами.

5. АТРИБУЦИЯ. Сейчас в углу стоит «© SPRINTER GO» — это нарушение: и OpenStreetMap,
   и Яндекс требуют указывать источник данных. Сделай так, чтобы подпись всегда
   показывала источник плиток, даже если в настройках её затёрли: если в шаблоне
   плиток есть openstreetmap — «© OpenStreetMap», если yandex — ссылка на Яндекс Карты,
   если cartocdn — добавляй «© CARTO». Своё название владелец может дописать рядом,
   но не вместо. Подпись мелкая, не мешает, ссылки кликабельны.`,
  },
]

const scr = await parallel(SCREENS.map(m => () => agent(m.prompt, { label: m.label, phase: 'Экраны' })))
log('Экраны готовы: ' + scr.filter(Boolean).length + ' из ' + SCREENS.length)

return { сервер: srv.filter(Boolean).length, экраны: scr.filter(Boolean).length }
