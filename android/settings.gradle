// Сборка приложения курьера. Ничего лишнего: только то, что нужно, чтобы
// Gradle нашёл плагин Android и сам Android SDK.
pluginManagement {
  repositories { google(); mavenCentral(); gradlePluginPortal() }
}
dependencyResolutionManagement {
  repositories { google(); mavenCentral() }
}
rootProject.name = "SprinterGoCourier"
include ":app"
