<?xml version="1.0" encoding="utf-8"?>
<resources>
  <!-- Светлая тема, как в самом сервисе. Заставка — жёлтый экран со знаком:
       ровно то, что человек видит, открывая сайт. -->
  <style name="SgTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
    <item name="android:windowBackground">@drawable/splash</item>
    <item name="android:statusBarColor">@color/accent</item>
    <item name="android:navigationBarColor">@color/paper</item>
    <item name="android:windowLightStatusBar">true</item>
    <item name="android:windowLightNavigationBar">true</item>
  </style>
</resources>
